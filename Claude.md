# ferrocache — Project Context

## Section 1: Evergreen

**What this is:** Distributed semantic cache for LLM applications, written in Rust. Single binary, multi-node via consistent hashing + gossip replication. Portfolio project for big-tech Core SWE interviews.

**Current phase:** Phase 6 in progress. M22 done (ring reassignment + failover). Next: M23 — anti-entropy (read repair).

**Completed work:**
- M1: axum scaffold (3 routes, tracing, env-based port)
- M2: hnsw_rs integration (DistCosine, side-table keyed by usize, dim lock)
- M3: WAL (NDJSON, fsync-per-insert, replay on startup, corrupt-line skip; WAL-first insert path; UUIDs stable across restarts)
- M4: config crate (TOML + env merge), request validation (4096 dim, 100KB resp, threshold range), /stats endpoint, dead-code cleanup
- M5: consistent hash ring (FNV-1a, 64 vnodes default) + chitchat gossip discovery, /cluster/status endpoint, single-node fallback
- M6: cluster-aware /query routing + /insert synchronous replication via reqwest, `?local=true` loop-prevention, coordinator UUID stamping, get_n_nodes replica walk
- M7: Dockerfile + docker-compose 3-node cluster + bash integration tests; fixed env-var `prefix_separator` config bug (latent since M4)
- M8: README + Mermaid diagram, criterion benchmarks (real numbers), stdlib-only Python client, GitHub Actions CI (check + integration), library/binary split for bench imports
- M9: simulation harness (`tests/simulate.py` + `simulate_no_ml.py`) — FAQ workload with sentence-transformers, 100% hit rate / 0 false positives at 0.90 threshold
- M10: drop-in SDK middleware (`wrap_openai`, `wrap_anthropic`) with proxy attribute delegation, fail-open on cache outage, env-var config; restructured Python client into a package
- M11: framework backends — `FerrocacheCache` (LangChain `BaseCache`) + `FerrocacheLLM` (LlamaIndex `CustomLLM` subclass); optional imports, fail-open default, custom `embed_fn` supported
- M12: MCP server (`ferrocache.mcp_server`) over stdio — 3 tools (`semantic_cache_lookup`, `semantic_cache_store`, `cache_status`), text-in/JSON-out (embedding handled internally), Claude Desktop + Claude Code setup docs
- M13: distribution — `pyproject.toml` with optional extras (`[openai]`, `[anthropic]`, `[langchain]`, `[llamaindex]`, `[mcp]`, `[all]`); base `pip install ferrocache` is zero-deps; root + Python LICENSE; release CI on `v*` tags publishes Docker image to GHCR and Python package to PyPI via OIDC trusted publishing
- M14: namespace partitioning — `SemanticIndex` is a `HashMap<model_id → NamespacedIndex>`, each with its own HNSW + side-table; `model_id` required on `/insert` and `/query`; Python integrations auto-derive `model_id` from default embed model; PyPI bumped to 0.2.0; pre-M14 WAL entries migrate to `legacy::unknown`
- M15: WAL compaction + snapshotting — `src/snapshot.rs` writes `magic+version+wal_seq+bincode(Vec<SnapshotEntry>)` atomically; WAL gained monotonic `sequence` numbers; startup loads snapshot then replays only `seq > watermark`; auto-compaction every 10K inserts (configurable) + manual `POST /admin/compact`; `CacheEntry` re-stores `embedding`+`query_text`
- M16: Prometheus `/metrics` (hand-written text exposition, no `prometheus` crate) — atomic counters with per-namespace `RwLock<HashMap>`, fixed 16-bucket `LatencyHistogram` (100µs → 10s), `/stats` JSON gains `counters` block, monitoring compose overlay (Prometheus + Grafana on :9090/:3100) with provisioned datasource and 8-panel ferrocache dashboard
- M17: bearer token auth — opt-in via `FERROCACHE_AUTH_TOKEN`; `subtle::ConstantTimeEq` compare; `/health` and `/metrics` exempt; inter-node forwards carry the same token; Python client reads env automatically; integrations gain `auth_token=` kwarg
- M18: cluster mTLS — opt-in via `FERROCACHE_CLUSTER__TLS__ENABLED`; rcgen 0.13 + rustls 0.23; second TLS listener on `internal_port` (default `port + 1000`) requires client cert chained to cluster CA; `WebPkiClientVerifier` rejects anonymous clients at handshake; `gen_certs` binary writes dev CA + per-node cert/key; production loads PEMs from `cluster.tls.{ca_cert,node_cert,node_key}_path`
- M19: replication retry (exponential backoff + ±20% jitter, retries on connect/timeout/5xx, never 4xx) + security audit (scrubbed peer IDs / WAL paths / snapshot paths from HTTP error bodies, kept in tracing logs) + `docs/security.md` threat-model/deployment-recipes/known-limitations + `clients/ferrocache-skill.md` LLM reference; `replication_retries_total` metric distinct from `replication_failures_total`
- M20: WAL group-commit — inserts push `WalCommand::Insert` to mpsc channel, single background flush task batches up to `wal_batch_size` (default 256) requests within `wal_batch_timeout_ms` (default 1ms), one fsync per batch; flush task is sole writer (compaction goes through same channel); 7.5× insert throughput at concurrency 100; new `tests/bench_concurrent.py` harness
- M21: phi accrual failure detector — `src/failure_detector.rs` with `PhiAccrualDetector` (sliding window of inter-arrivals, normal CDF via Abramowitz & Stegun erf, `phi = -log10(1 - F(elapsed))`), `PeerStatus::{Alive, Suspected, Dead}` state machine; heartbeats piggyback on chitchat reconciler; `peer_health` map in `/cluster/status`, `ferrocache_peer_phi` gauge in `/metrics`; soft routing skips Dead peers (query → miss instead of 502, insert fan-out → degraded replication)

**Module map:**
- lib.rs — re-exports modules so benches and external consumers can import
- main.rs — entry, tracing init, config load, WAL replay, optional cluster init, serve
- server.rs — router, handlers, request validation, routing/replication coordination, tests
- index.rs — SemanticIndex (hnsw_rs wrapper + HashMap side-table), replay_entry
- wal.rs — `Wal` (append, fsync, sequence, truncate, replay) + `GroupCommitWal` (mpsc-fed flush task that batches writes and fsyncs once per batch); `WalCommand::{Insert,Compact}`, `InsertError::{Wal,Index}`, `GroupCommitConfig`; flush task owns the only `Wal` handle and the index write lock
- models.rs — request/response DTOs (InsertRequest carries optional `uuid`)
- config.rs — FerrocacheConfig (port, wal_path, hnsw, cluster, compact_interval_inserts, auth_token, wal_batch_size, wal_batch_timeout_ms), HnswConfig, ClusterConfig (gossip_addr, api_addr, replication_factor, seed_nodes, tls, max_replication_retries)
- state.rs — AppState { node_id, index, wal: GroupCommitWal, wal_path, snapshot_path, hnsw_config, cluster, router, replication_factor, metrics, auth_token, max_replication_retries }
- auth.rs — `auth_middleware` (axum `from_fn_with_state`); constant-time `subtle::ConstantTimeEq`; `/health` + `/metrics` exempt; only installed when `auth_token` is non-empty
- tls.rs — `TlsBundle`, `generate_ca`/`generate_node_cert` (rcgen 0.13), `load_or_generate` (disk vs in-memory dev), `build_server_config` (rustls 0.23 mTLS — `WebPkiClientVerifier` requires a client cert chained to the cluster CA), `install_default_crypto_provider` (idempotent aws-lc-rs install); used by `main.rs` for the internal listener and `router.rs` for the reqwest identity
- bin/gen_certs.rs — dev-cert generator binary (`cargo run --bin gen_certs [node_ids...]`); writes `./certs/ca.pem` + `./certs/<node_id>/{cert,key}.pem`; intentionally not built into the production Docker image
- ring.rs — HashRing (BTreeMap<u64, node_id>, FNV-1a, virtual nodes, get_n_nodes for replica walk)
- cluster.rs — ClusterState wrapping chitchat; `reconcile_step` (extracted free fn so tests drive it without UDP) feeds the phi accrual detector then drives ring membership: ring ADDs come from chitchat snapshot vs ring-set diff (with re-join detection vs `dead_nodes`), ring REMOVALS come from detector `Dead` status (M22), never from chitchat `live_nodes` directly; `dead_nodes: HashSet<String>` tracks ring-removed peers for `/cluster/status`
- failure_detector.rs — `PhiAccrualDetector` (sliding window of inter-arrival times per peer, normal CDF via Abramowitz & Stegun erf, phi=-log10(1-F(elapsed))); `PeerStatus::{Alive, Suspected, Dead}`; injectable `_at(Instant)` methods so tests don't depend on wall-clock; passive — never sends heartbeats, only observes
- router.rs — ClusterRouter (reqwest client) — forward_query / forward_insert return typed `reqwest::Result` so the `forward_with_retry` helper can classify failures (retry on connect errors / timeouts / 5xx; never on 4xx); ±20% jitter from `rand::thread_rng()`; metric `ferrocache_replication_retries_total` increments inside the helper
- snapshot.rs — `SnapshotEntry` + `write_snapshot`/`read_snapshot` (magic+version+wal_seq+bincode); `compact()` helper that writes the snapshot and truncates the WAL atomically
- metrics.rs — `Metrics` (atomic counters + per-namespace `RwLock<HashMap<NamespaceMetrics>>`), `LatencyHistogram` (16 fixed buckets 100µs–10s), `render()` to Prometheus text exposition format
- benches/cache_bench.rs — criterion: insert / query_hit / query_miss / insert_with_wal / snapshot_write_10k
- tests/bench_concurrent.py — concurrent throughput harness (HTTP via FerrocacheClient at concurrency 1/10/50/100; insert + query_hit + query_miss); `make bench-concurrent`
- clients/python/ferrocache.py — stdlib-only Python client (urllib + json)

**Architecture decisions (append-only, one line each):**
- Client computes embeddings externally; ferrocache stores/compares f32 vectors
- Crates: tokio, axum 0.7, hnsw_rs, chitchat 0.10, tracing, serde_json, config, reqwest
- WAL format: newline-delimited JSON, replayed on startup, fsync per insert
- Consistent hashing key: u64 from first 8 bytes of embedding (first 2 f32 values, big-endian)
- Cosine distance: hnsw_rs DistCosine returns 1-similarity; we convert
- Side-table: HashMap<usize, CacheEntry> keyed by hnsw internal id; UUID inside CacheEntry
- WAL is source of truth; production insert path is WAL-first then replay_entry
- insert() (UUID-generating) is #[cfg(test)] only; production uses replay_entry
- Config priority: env vars (FERROCACHE_ prefix, `_` prefix-sep, `__` section-sep, `,` list-sep) > ferrocache.toml > defaults
- tokio::sync::RwLock for index; WAL is owned by a single background flush task (no shared mutex)
- Validation limits (server.rs constants): MAX_EMBEDDING_DIM=4096, MAX_RESPONSE_BYTES=102_400
- Threshold accepted range on /query: 0.0..=1.0
- Hash ring: FNV-1a (cross-process deterministic), 64 virtual nodes per physical node by default
- Cluster mode is opt-in via `cluster.enabled`; `false` keeps Phase 1 behavior bit-identical
- Ring reconciler runs every 2s in a background task; chitchat gossip interval = 1s
- Each node advertises its API addr via chitchat KV under key `api_addr`; reconciler reads peers' addrs
- Routing: `?local=true` skips ring lookup (used by forwarded requests + debugging); coordinator stamps UUID before fan-out
- Replication: synchronous, primary-or-coordinator + (replication_factor-1) replicas; any replica failure → 502
- Forward failures return 502 (BAD_GATEWAY) to distinguish "peer failed" from local 500
- Docker runtime image needs `libgomp1` (hnsw_rs → rayon → OpenMP)
- `seed_nodes` is parsed from a comma-separated env string via config-rs `list_separator(",")`+`with_list_parse_key`
- `Wal::open` mkdir's parent dir so containers can mount an empty `/data` volume
- Crate is library + binary (lib.rs re-exports modules); `cargo build --release --bin ferrocache` for prod images
- Benchmarks via criterion; Python client uses only stdlib (urllib+json)
- Python package on PyPI with optional extras; Docker image on GHCR; release CI on version tags
- Index is namespace-partitioned by `model_id`; each namespace has its own HNSW instance + side-table
- `model_id` is required on `/insert` and `/query`; old WAL entries without it default to `legacy::unknown`
- `model_id` format convention: `model_name::dimension` (e.g. `all-MiniLM-L6-v2::384`)
- Cross-namespace queries are impossible by construction — vectors from different models never compare
- Snapshot format: `magic (FERROSNA) + version (u64=1) + wal_sequence (u64) + entry_count (u64) + bincode-encoded Vec<SnapshotEntry>`; written atomically via temp+rename so a crash never corrupts the prior snapshot
- Compaction: snapshot the side-table entries, then truncate the WAL — HNSW is rebuilt from stored embeddings on load (no need to serialize the graph itself)
- WAL entries carry a monotonic `sequence: u64` that never resets — startup skips entries `<= snapshot_watermark` so the WAL tail replay is bounded
- Auto-compaction every N inserts (default 10K via `compact_interval_inserts`, 0 disables); manual via `POST /admin/compact`
- `CacheEntry` again stores `embedding` + `query_text` so snapshots can dump the full record (re-added in M15; was dropped in M4)
- Prometheus `/metrics` is hand-written (no `prometheus`/`metrics` crate); atomic counters with `Ordering::Relaxed` + a fixed-bucket `LatencyHistogram`
- Per-namespace hit/miss/insert counters carry a `namespace` label; global query/insert latency histograms have 16 buckets (100µs → 10s) plus a `+Inf` bucket
- `/stats` JSON gained a `counters` object so users without Prometheus can still read the same numbers
- Monitoring stack lives in `monitoring/` as a docker-compose overlay (`-f docker-compose.yml -f monitoring/docker-compose.monitoring.yml`); base cluster runs unchanged without it
- `CacheEntry` again stores `embedding` + `query_text` so snapshots can dump the full record (re-added in M15; was dropped in M4)
- Bearer token auth via `FERROCACHE_AUTH_TOKEN` env var; disabled when unset (opt-in, backward compatible)
- Auth exempts `/health` and `/metrics` (load balancers and Prometheus need unauthenticated access)
- Token comparison uses `subtle::ConstantTimeEq` to prevent timing attacks; the token value is never logged at any level
- Inter-node replication forwards include the same bearer token (shared-secret cluster model — all nodes carry the same `FERROCACHE_AUTH_TOKEN`)
- mTLS between cluster nodes via rustls 0.23 + rcgen 0.13; opt-in via `cluster.tls.enabled`; second TLS listener on `internal_port` (default `port + 1000`) serves the same router; public port stays plain HTTP
- Dev mode (no cert paths) auto-generates a self-signed CA + leaf in memory; production loads PEMs from `cluster.tls.{ca_cert,node_cert,node_key}_path`
- TLS reqwest client trusts ONLY the cluster CA (system roots disabled) and presents the node leaf as client identity — i.e. mTLS, not just TLS
- Gossip (chitchat UDP) remains unencrypted: it carries only ring metadata (node IDs + forwarding addresses), not cached data
- `cargo run --bin gen_certs [node_ids...]` writes `./certs/ca.pem` + `./certs/<node>/{cert,key}.pem` for local clusters; the production Dockerfile builds only `--bin ferrocache`, so `gen_certs` never ships in images
- Replication retry: exponential backoff (50ms × 2^attempt, ±20% jitter, capped at 5s), max 3 attempts by default; retries on connect errors + timeouts + 5xx only, never on 4xx (deterministic failures)
- `ferrocache_replication_retries_total` metric tracks retry pressure separately from final failures — retries that succeed do NOT count as failures, so dashboards can distinguish "noisy network, eventually succeeded" from "peer permanently down"
- WAL group-commit (M20): inserts push `WalCommand::Insert` to an mpsc channel; a single background flush task owns the `Wal`, batches up to `wal_batch_size` requests (default 256) or `wal_batch_timeout_ms` (default 1ms) elapses, writes the entire batch in one `write(2)` and one `fsync(2)`, then takes the index write lock once and inserts every entry; per-request oneshot replies fire after the lock is released. `wal_batch_size = 1` degrades to per-insert fsync (pre-M20 behavior) and is what tests use.
- Insert throughput scales with concurrency under group-commit (measured: 7.5× at concurrency 100 vs per-insert fsync); single-client insert is slightly slower (batch timeout adds ~1ms wait); query throughput is unaffected (read path doesn't fsync)
- The flush task is the SOLE writer to the WAL — `compact` is also a `WalCommand` so it serializes with insert flushes (FIFO via the same channel); auto-compaction's `inserts_since_compact` counter lives in the flush task only (no atomics, no locking, single producer)
- Insert handler error surface: `InsertError::Wal` → 500 "persistence failed" (paths/error chain stay in logs), `InsertError::Index` → 500 with the underlying message (e.g. "dimension mismatch") preserving M19 audit shape
- Phi accrual failure detector per peer: sliding window of heartbeat inter-arrivals (default 100 samples), normal CDF, phi = -log10(1-F(elapsed)); threshold 8.0 default (Cassandra/Akka standard); std-dev floored at 100ms so a perfectly regular heartbeat stream still has a defined CDF
- Heartbeats piggybacked on chitchat gossip — the existing 2s reconciler tick records a heartbeat for each peer chitchat reports as live, then runs `check_all`; no separate heartbeat task
- Three peer states: `Alive → Suspected (phi ≥ threshold) → Dead (phi ≥ 2×threshold)`; recovery to `Alive` on any new heartbeat (Dead is not permanent — ring removal is M22's job)
- Dead peers skipped in query routing (immediate miss instead of retry-then-502) and insert fan-out (degraded replication with warn log naming `dead_peers` and `effective_replicas`); soft optimization, retry logic still runs if a "dead" peer races back into a request
- Per-peer phi exposed as `ferrocache_peer_phi{peer=...}` gauge plus `ferrocache_peers_suspected` / `ferrocache_peers_dead` gauges in /metrics; `peer_health` map in /cluster/status JSON
- Ring membership decoupled from chitchat liveness (M22): ring removals driven exclusively by phi accrual `Dead` status; `Suspected` peers stay in the ring (might recover); `chitchat.live_nodes()` controls discovery + KV propagation only
- Dead nodes tracked in `dead_nodes: HashSet<String>` on `ClusterState`, surfaced via `/cluster/status.dead_nodes`; re-joining nodes (reappear in chitchat snapshot) are added back to ring AND removed from the dead set on the same reconcile tick
- Ring reassignment is automatic and zero-data-movement: removing a dead node's virtual nodes from the `BTreeMap`-backed `HashRing` makes its arc fold into the clockwise successor, which is already a replica thanks to `replication_factor=2` — the cache property (no durable single-source-of-truth contract) means we don't need anti-entropy to ship data; missed entries are recomputed by the LLM on the next miss
- `failure_detector.remove_peer` is NOT called from the reconciler — the detector keeps history across chitchat-driven drops so phi can keep climbing into Dead even after gossip stops reporting the peer; only when an operator actually removes a node from the cluster would `remove_peer` apply (out of scope)
- Re-joined nodes serve stale data until the next anti-entropy pass (M23 will add this); stale misses are acceptable in cache semantics — the client re-calls the LLM, the response is cached on the new owner, ring is consistent again
- `dead_node_removal_enabled` config flag (default `true`) supports a monitoring-only mode where phi reports without ring mutation — useful for canary rollouts of the failover behaviour
- Ring change observability: `ferrocache_ring_changes_total` counter (every add or remove from the reconciler) + `ferrocache_ring_members` gauge mirror what's in the ring; bumped from `Metrics::record_ring_change()` inside `reconcile_step`

**Non-negotiable constraints:**
- Auth (bearer token) and cluster mTLS are both opt-in; public-port TLS handled by reverse proxy
- No UI
- No direct OpenAI/Anthropic API calls inside ferrocache
- Use tokio, not async-std
- Every new module gets unit tests before moving on

**Session log rules:**
- Keep only the last 2 session logs below
- When adding a new log, delete the oldest if there are already 2
- Summarize deleted sessions as one-line entries under "Completed work" above

## Section 2: Rolling Session Log (last 2 sessions only)


### 2026-05-02 — Mission 21: Phi Accrual Failure Detection
**Built:** New `src/failure_detector.rs` (added to `lib.rs`) — `PhiAccrualDetector { peers: RwLock<HashMap<String, PeerState>>, phi_threshold: f64, window_size: usize, min_std_dev_ms: f64 }` with `PeerState { heartbeat_history: VecDeque<f64>, last_heartbeat: Instant, status: PeerStatus }`; `PeerStatus::{Alive, Suspected, Dead}` with `#[serde(rename_all = "lowercase")]` so /cluster/status emits `"alive"`/`"suspected"`/`"dead"`. Module exports `DEFAULT_PHI_THRESHOLD = 8.0`, `DEFAULT_WINDOW_SIZE = 100`, `DEFAULT_MIN_STD_DEV_MS = 100.0`. Public API is async (peers map is `tokio::sync::RwLock`): `record_heartbeat(node_id)` / `record_heartbeat_at(node_id, Instant)` (test hook for time injection), `phi(node_id) -> Option<f64>` / `phi_at(...)`, `check_all() / check_all_at(at)` (the state-machine driver), `remove_peer`, `peer_statuses`, `alive_peers`/`suspected_peers`/`dead_peers`, `snapshot()/snapshot_at(at) -> Vec<(String, PeerStatus, f64)>` (used by both /metrics and /cluster/status). Phi math: `mean = sum/n` of inter-arrivals, `variance = sum((x-mean)^2)/n` (population variance, not sample — Cassandra precedent), `std_dev = max(variance.sqrt(), min_std_dev_ms)`, `cdf = normal_cdf(elapsed, mean, std_dev)`, `p = (1.0 - cdf).max(f64::MIN_POSITIVE)`, `phi = -p.log10()`. The MIN_POSITIVE floor caps phi at ~307; without it the erf approximation saturates `cdf` to literal 1.0 for z > ~6 and phi blows up to `inf`. CDF: `0.5 * (1.0 + erf(z / sqrt(2)))`. Erf: Abramowitz & Stegun 7.1.26 — 5-term polynomial in `t = 1/(1 + 0.3275911 * |x|)`, max error ~1.5e-7. State-machine in `check_all_at`: holds a write lock, iterates peers, for each computes phi at `at`, then transitions per `match state.status { Alive => if phi ≥ threshold → Suspected; Suspected => if phi ≥ 2*threshold → Dead; Dead => {} }`. Recovery is in `record_heartbeat_at` — any heartbeat resets `status = Alive` regardless of prior state (Dead is not permanent; ring removal is M22's job). `ClusterConfig` gained 3 fields: `phi_threshold: f64` (default 8.0), `phi_window_size: usize` (default 100), `phi_min_std_dev_ms: f64` (default 100.0); env vars `FERROCACHE_CLUSTER__PHI_*`; all wired through `set_default` in the builder so env-var overrides round-trip. `ClusterState` gained `failure_detector: Arc<PhiAccrualDetector>` constructed in `ClusterState::new` from config, plus `failure_detector()` accessor + `peer_status(node_id) -> PeerStatus` convenience that returns `Alive` for unknown peers (so callers don't need a special "untracked" branch). Reconciler now calls `record_heartbeat(node_id)` for each peer chitchat reports as live (excluding self), THEN calls `check_all().await` — order matters: heartbeats reset phi for live peers first, then the check picks up only those that didn't heartbeat. After the check, peers transitioned to Suspected/Dead are logged at warn/error respectively. When chitchat itself drops a peer from `live_nodes()`, the reconciler also calls `failure_detector.remove_peer(id)` so we don't accumulate stale entries. `/cluster/status` JSON gained `peer_health: HashMap<String, PeerHealth { status, phi }>` populated from `failure_detector.snapshot()`; serialized with `skip_serializing_if = "HashMap::is_empty"` so single-node mode (no detector) silently omits the field. `/metrics` gained `ferrocache_peer_phi{peer="..."}` (gauge, sorted by peer id for deterministic output), `ferrocache_peers_suspected` (gauge), `ferrocache_peers_dead` (gauge); `Metrics::render` signature gained a third arg `peer_phi: &PeerPhiSnapshot` (`Vec<(String, PeerStatus, f64)>` typedef), sourced from `failure_detector.snapshot()` in the metrics handler and `Vec::new()` in single-node mode. **Soft routing** (the bridge to M22, not full failover): in `query_handler`, after ring lookup, before `forward_query`, check `cluster.peer_status(target_id) == Dead` — if so, return `200 { hit: false }` immediately (skip the doomed retry chain). In `insert_handler`'s fan-out loop, dead replicas are filtered out into `skipped_dead: Vec<&str>` and the loop continues with surviving replicas only; if any replicas were skipped, log `tracing::warn!(dead_peers, effective_replicas, "replication degraded")`. The replication factor degrades silently from the client's perspective (still returns 200), only operators see it via logs + metrics. Tests: 14 in `failure_detector.rs::tests` covering — initial phi=None for unknown peer, heartbeat registers as Alive, phi grows with silence (using `phi_at(now+5s)`), Alive→Suspected transition (30s silence past threshold), Suspected→Dead transition (continued silence past 2× threshold via two `check_all_at` calls), recovery from Suspected via heartbeat, recovery from Dead via heartbeat, `check_all_returns_correct_statuses` (3 peers — alive, suspected, dead; uses 1000ms `min_std_dev` so phi sits in the (8, 16) band for the suspected peer rather than saturating), window_size bounds (20 heartbeats into a 5-sample window), remove_peer, erf approximation against known values (erf(0)≈0, erf(1)≈0.8427, erf(2)≈0.9953, erf(-1)≈-0.8427), normal_cdf at mean and mean+3σ, phi_at unknown returns None, alive_peers vs suspected_peers filtering. 1 new server test: `test_cluster_status_includes_peer_health` builds the response shape directly (ClusterState's chitchat handle is too heavy for unit tests) and asserts `peer_health.peer-A.status == "alive"` + `phi` is a number. `ferrocache.toml` gained a commented `[cluster]` block with the three phi knobs.

**Verified:**
- `cargo build` (debug) — clean.
- `cargo test` — 118/118 pass (was 103; +14 detector + 1 server peer_health). 14/14 detector tests are deterministic via `_at(Instant)` time injection, no flaky wall-clock waits.
- `cargo clippy --all-targets -- -D warnings` — clean.
- `cargo fmt --check` — clean (after one auto-fmt pass).
- `python3 -m unittest discover tests` — 39/39 pass (no Python changes).
- `docker compose up -d --build && ./tests/cluster_integration.sh && docker compose down -v` — 20/20 pass; backward compat preserved.
- Live smoke test on the docker cluster: started 3 nodes, `/cluster/status` showed `peer_health` with both peers alive at phi ≈ 1e-5 (heartbeats every 1s, mean inter-arrival ~1s, std-dev floored at 100ms gives a tiny but non-zero z-score even right after a heartbeat). Stopped node3, observed node3 disappear from `peer_health` and `nodes` within ~25s — chitchat's own failure detector is faster than phi accrual at default config, so `live_nodes()` excluded node3 before our 8-threshold fired. The window where phi-accrual is the operative signal is the gap between "chitchat's last heartbeat from peer" and "chitchat removes peer from live_nodes()"; for the M21 deliverable this is fine because both detectors agree on the eventual state.

**Key decisions:**
- Detector is **passive** — it only observes the existing chitchat heartbeat. The brief was explicit about this; we don't add a separate heartbeat protocol. The reconciler's 2s tick *is* the heartbeat input, so phi-accrual's resolution is bounded by chitchat's gossip interval (1s) plus the reconciler's pulling interval (2s). For LAN with 1-2s reconciler ticks and threshold 8, expected detection time is ~10-20s.
- **`_at(Instant)` test hooks instead of a clock trait.** The brief offered both options. `Instant` is opaque (you can't construct one for an arbitrary moment), but `record_heartbeat_at` lets tests pass `start + i * Duration::from_millis(1000)` to seed a regular history, and `check_all_at(now)` lets them control the "current time" for the phi computation. No mock-clock crate, no trait abstraction in production code, deterministic test results.
- **`min_std_dev_ms` floor at 100ms (default).** Without it, a perfectly regular heartbeat stream (every 1000ms exactly) gives variance=0 → std_dev=0 → CDF degenerates to a step function (phi is 0 if elapsed < mean, ∞ otherwise). The floor turns this into "tolerate up to ~100ms jitter as still normal" which matches LAN reality. The test `test_check_all_returns_correct_statuses` deliberately uses `min_std_dev_ms = 1000.0` to widen the (Suspected, Dead) phi band so the test can place a peer cleanly inside it — at the default 100ms floor, a 7s silence with 1s mean gives z=60σ → phi saturates at ~307, jumping straight past the Suspected zone.
- **Phi caps at ~307, not infinity.** The clamp `(1.0 - cdf).max(f64::MIN_POSITIVE)` is the numerical guard. Cassandra/Akka use the same pattern. Without it, `1.0 - 1.0 = 0.0` → `log10(0) = -inf`. With it, phi is bounded by `-log10(MIN_POSITIVE) ≈ 307`. All comparisons in the state machine work correctly because 307 ≥ any reasonable threshold.
- **Recovery from Dead is allowed** (not just Suspected → Alive). The brief explicitly said "Recovery from Dead to Alive also happens on the next heartbeat — dead is not permanent (that's for M22's ring removal to decide)." Implemented exactly: `record_heartbeat_at` sets `status = PeerStatus::Alive` unconditionally. Tested by `test_recovery_from_dead`.
- **`peer_status()` returns Alive for unknown peers.** The detector starts empty; peers get registered the first time the reconciler sees them in chitchat's live set. So during the first 2s window after startup, every peer is "unknown" to the detector. Returning `Alive` (rather than `None` or `Suspected`) means the routing fast-path doesn't gate on detector readiness — the detector is purely additive: when it knows something, it reports it; when it doesn't, the system behaves exactly as pre-M21.
- **Soft routing returns `hit:false` for dead peers, not 502.** The brief made the case: "a wrong cache miss is cheap; a 502 isn't." A miss just causes the LLM to be re-called (correctness preserved); a 502 propagates an error to the caller. For inserts, the same logic shifts to "degraded replication" — fewer copies, but writes still succeed, only the warn log shows the degradation.
- **`failure_detector.remove_peer` on chitchat-driven ring removal.** When chitchat drops a peer from `live_nodes()`, the reconciler removes it from the ring AND from the failure detector. Otherwise the detector would accumulate ghost entries forever (a node that left and never came back would still have a `PeerState` entry with phi rising to 307). The brief left this implicit; making it explicit prevents memory growth in long-lived clusters with churn.
- **`peer_health` field on /cluster/status uses `skip_serializing_if = "HashMap::is_empty"`.** Single-node mode has no detector, so the field is omitted entirely from the JSON rather than emitting an empty `{}`. Existing test `test_cluster_status_single_mode` was extended with `assert!(body.get("peer_health").is_none())` to lock this in.

**Deviations:**
- Brief showed `peers: RwLock<HashMap<String, PeerState>>` with no async qualifier; used `tokio::sync::RwLock` for compatibility with the rest of the cluster code (which awaits on locks). All public methods are async as a consequence.
- Brief proposed `Metrics::render` accept "an optional `&PhiAccrualDetector`". Used `&PeerPhiSnapshot` (a `Vec<(String, PeerStatus, f64)>` typedef) instead — the metrics handler computes the snapshot once and passes it in, which decouples `Metrics::render` from the failure detector module entirely. Better testability (the existing `test_render_*` tests just pass `&Vec::new()`); same observable behavior.
- Brief's `PeerState` had `last_heartbeat: Instant` directly. Implemented as such, with `saturating_duration_since` so a clock skew between threads can't underflow into a panic — defensive but free.
- Brief said `phi_window_size = 100` default. Used same; clamped to `max(2)` in `PhiAccrualDetector::new` so a config typo of `0` or `1` doesn't crash variance computation.
- Brief implied chitchat's own failure detector is slow enough that phi-accrual is the dominant signal. In practice, default chitchat in this codebase (10s gossip interval, 25s detection) runs at roughly the same speed as phi-threshold=8 with 2s reconciler. They're complementary: chitchat removes the node from the ring (transport/membership truth), phi-accrual reports the gradient (operator-facing observability). The smoke test bears this out.
- Brief's `test_phi_increases_with_silence` proposed comparing phi immediately after the last heartbeat against phi N seconds later. Implemented exactly; passed cleanly without flakiness because both phi values are computed at deterministic `Instant`s.

**Open / next:**
- **Ring reassignment is M22.** This mission deliberately stopped at "detect and skip". The next mission's job: when phi marks a peer Dead, reassign its ring slots to the next replica in the walk so queries find data on the new owners. That requires either rebalancing the existing copies (anti-entropy / read-repair) or accepting the data loss until the dead peer comes back. Cassandra's "hinted handoff" is the canonical solution; ferrocache will likely do something simpler given it's a *cache* (data loss on dead nodes is acceptable — the LLM round-trip recomputes).
- **Chitchat's failure detector overlaps with ours.** Currently both run; they don't conflict but one of them is redundant. A cleaner future design would either disable chitchat's detector (use chitchat purely as a transport for KV gossip) or use chitchat's detector exclusively (drop phi-accrual). The portfolio value of phi-accrual is the algorithmic depth, so the redundancy is intentional for now.
- **The detector's RwLock is held during phi math.** Computing phi for N peers in `check_all_at` is O(N × window_size) under a write lock. At default settings (100 samples, ≤10 peers) this is ~1µs of work — fine. For larger clusters or larger windows, splitting "snapshot peer state under read lock, compute outside, transition under write lock" would scale better. Not currently needed.
- **No alerting integration.** The phi gauge is exposed on /metrics, but there's no preconfigured Grafana alert in `monitoring/`. Adding a "alert when ferrocache_peers_suspected > 0 for 1m" rule would close the loop for operators using the bundled monitoring stack.
- **The smoke test couldn't trigger the Dead state through the user-facing API** because chitchat dropped node3 from `live_nodes()` before phi accumulated past the threshold. This is expected behavior and OK — but it means the soft-routing change (skip Dead in query/insert) is exercised mostly by phi math hitting threshold, not by the chitchat-faster path. M22 will likely want to keep dead peers in the ring longer (decoupling ring membership from chitchat liveness) to give phi a chance to drive the routing decision.

### 2026-05-02 — Mission 22: Ring Reassignment + Automatic Failover
**Built:** Decoupled ring membership from chitchat's liveness vote. (1) `ClusterState` gained `dead_nodes: Arc<RwLock<HashSet<String>>>` (peers the detector confirmed Dead and the reconciler removed from the ring) plus accessor `dead_nodes() -> Vec<String>` (sorted for deterministic `/cluster/status`). (2) `ClusterState::new` now takes an `Arc<Metrics>` arg so the reconciler can bump `ring_changes_total` on every add/remove; `main.rs` constructs `metrics` BEFORE the cluster init and passes the Arc through. (3) New `pub(crate) async fn reconcile_step(snapshot, self_node_id, self_api_addr, ring, addrs, dead_nodes, failure_detector, metrics, dead_node_removal_enabled)` — the entire reconciler's state machine extracted into a free function so tests drive it with synthetic snapshots (no UDP, no gossip handshake, no real time). The spawned `spawn_ring_reconciler` task is now a thin loop that snapshots chitchat then calls `reconcile_step`. State machine, in order: (a) update `addrs` map from snapshot (peers may re-advertise); (b) `record_heartbeat` for every non-self peer in snapshot — this resets Dead peers back to Alive in the detector if they re-joined; (c) compute `to_add = snapshot.keys() - ring.nodes()`, add them, and for each one, if it was in `dead_nodes` log "node re-joined cluster" + remove from the dead set, otherwise log "new node added"; bump `ring_changes_total` per add; (d) `failure_detector.check_all()` — log Suspected (warn) + Dead (error); (e) when `dead_node_removal_enabled`, find peers the detector reports as `Dead` that are still in the ring, remove them from ring + addrs, add to `dead_nodes`, bump `ring_changes_total`. Crucially: the reconciler never calls `failure_detector.remove_peer` (M21 used to). The detector keeps history across chitchat-driven drops so phi can keep climbing into Dead even after gossip stops reporting the peer. Only the user-driven "remove this node permanently" flow would call `remove_peer` — out of scope for M22. (4) `ClusterStatusResponse` gained `dead_nodes: Vec<String>` field (`#[serde(skip_serializing_if = "Vec::is_empty")]` so single-node mode still omits it cleanly); `cluster_status_handler` populates from `cluster.dead_nodes().await`. (5) `Metrics` gained `ring_changes_total: AtomicU64` + `record_ring_change()`, plus a new `ferrocache_ring_changes_total` counter and `ferrocache_ring_members` gauge in `render()` (gauge is an alias for the existing `cluster_nodes` value but distinct in name for dashboard clarity). (6) `ClusterConfig` gained `dead_node_removal_enabled: bool` (default `true`, env `FERROCACHE_CLUSTER__DEAD_NODE_REMOVAL_ENABLED`). When `false`, phi is still computed/exposed/logged but ring removal is suppressed — monitoring-only canary mode. (7) The retry-then-502 collapse path is gone for dead peers: query routing now sees `peer_status == Dead` (M21 soft skip, returns 200 miss immediately) AND, with M22, the dead peer is no longer even in the ring (queries route to the clockwise successor, which is already a replica from `replication_factor=2`). The two paths are complementary: M21 covers the gap between phi=Dead and ring removal (≤2s reconciler tick); M22 covers the post-removal steady state. (8) `ferrocache.toml` gained a commented `dead_node_removal_enabled = true` line. (9) `tests/cluster_integration.sh` got a new "Test 7: Node failure and ring reassignment (M22)" — inserts → stops node3 → waits 35s for failure detection → asserts `/cluster/status` 200, query 200 (not 502), insert 200 (not 502) → restarts node3 → asserts `/cluster/status` 200 after re-join. Timing-tolerant by design: the load-bearing assertions are non-502 status codes, not exact timing of the Dead transition.

**Verified:**
- `cargo test` — 127/127 pass (was 118; +9 new: 6 cluster reconcile state-machine tests, 1 ring redistribution test, 1 server `dead_nodes` shape test, 1 ring-after-failover routing test).
- `cargo clippy --all-targets -- -D warnings` — clean (after one round factoring a complex test fixture tuple into a `ReconcileFixture` typedef + alias the unused destructured `det` in `test_suspected_stays_in_ring`).
- `cargo fmt --check` — clean.
- `python3 -m unittest discover tests` — 39/39 pass (no Python changes).
- `docker compose up -d --build && ./tests/cluster_integration.sh && docker compose down -v` — 25/25 pass (was 20; +5 new failover assertions).
- Live failover smoke test on the docker cluster: started 3 nodes, killed node3, observed `/cluster/status` over time. At T+30s phi for node3 was 5.1 (climbing toward threshold 8); at T+90s node3 transitioned to `peer_health.node3.status = "dead"` with phi saturated at 307.6, was removed from `nodes` (count dropped 3→2), and appeared in `dead_nodes: ["node3"]`. The state-machine progression is exactly what the algorithm dictates and the JSON shape matches the brief.

**Key decisions:**
- **Reconciler is a free function**, not a method on `ClusterState`. Reasoning: the spawned task captures Arcs + cloned data, not a `&ClusterState`, and tests want to call it with synthetic inputs. Making it `pub(crate) async fn reconcile_step(...)` lets the spawn loop AND `#[cfg(test)]` exercise the same code path. The 9-arg signature is ugly but the alternative (a fixture struct) adds boilerplate to one production caller for the benefit of zero non-test callers — wash.
- **The detector keeps history across chitchat-driven drops.** This is the M21 → M22 inversion: M21 called `failure_detector.remove_peer` from the reconciler "to keep memory bounded"; M22 needs the opposite — phi must keep accumulating after chitchat drops the peer, otherwise we never reach `Dead` and never trigger the ring removal. The "long-running clusters with churn accumulate ghost detector entries" concern from M21 is addressed elsewhere: a real M22 run's detector entries are bounded by ring size (peers we've ever seen + are still in `dead_nodes`), and re-joins reset the entry rather than create a new one.
- **`Suspected` peers stay in the ring.** The brief was emphatic: only `Dead` triggers removal. This avoids flapping (phi can dip back below threshold during transient network blips), and it gives the detector the natural cooldown of "cross threshold once → Suspected → cross 2× threshold → Dead" before any ring mutation happens.
- **Re-joins are detected via the `dead_nodes` set, not via the detector's status.** When chitchat reports a peer that's in `dead_nodes`, we treat it as a re-join: add to ring + remove from dead set + log "re-joined". Using the detector's status would mean checking "was this peer Dead a moment ago?" which is unreliable because `record_heartbeat` already flipped it to Alive. The dead set is the durable record of "this peer was ring-removed", which is the right signal for re-join detection.
- **No data movement on ring reassignment.** This is the cache-vs-database insight: when node C dies, its arc folds into the clockwise successor (some node A), and A already has the data because of `replication_factor=2`. The new test `test_query_routes_to_replica_after_failover` proves this exact invariant at the ring layer — find an embedding whose primary is C and replica is A; remove C; assert the new primary equals A. No anti-entropy, no read repair, no data shipping needed.
- **`dead_node_removal_enabled = false` is monitoring-only mode.** Phi is computed, logged, exposed in `/metrics` and `/cluster/status.peer_health`, but the ring stays static. Useful for an operator who wants to deploy phi-based detection first and observe what it would do, before flipping the switch on automatic ring mutation. Tested by `test_dead_node_removal_disabled`.
- **`ring_changes_total` counts every add and every remove.** Including the initial self-add when a fresh state starts up. The brief left this implicit; making it count every mutation gives operators a clean signal for "the ring just changed, check Grafana for what". The test `test_ring_change_metric` locks in the exact count: 2 adds (self+A) on first reconcile, 1 add (B) on second, 1 remove (A driven Dead) on third = 4 total.
- **Integration test asserts non-502, not exact `dead_nodes` membership.** Phi accrual + chitchat detection together can take 30-60+ seconds; asserting "after sleeping 35s, dead_nodes contains node3" would be flaky on slow CI runners. Asserting "queries return 200" works because both M21 (soft skip during phi-accrual gap) and M22 (post-removal routing) produce 200 — whichever path fires first, the test passes. Verified via the live smoke test that the eventual behavior is correct.

**Deviations:**
- Brief sketched the reconciler change as inline edits to the existing `spawn_ring_reconciler` body. Refactored into the standalone `reconcile_step` instead — strictly more testable, same behavior, ~10 lines of glue.
- Brief called out a separate test file `src/failover.rs`. Kept the tests in `cluster.rs::tests` since that's where `reconcile_step` lives — single-file cohesion beats a parallel module that imports its own crate's internals.
- Brief proposed `ring_members` as a separate gauge "read from `ring.node_count()` at render time". Implementation re-uses the existing `cluster_nodes` value (which IS `ring.node_count()`) and emits both gauges with the same value — `ring_members` is the dashboard-friendly name, `cluster_nodes` is the legacy name kept for back-compat. Single source of truth, two emit lines.
- Brief showed `dead_nodes: Arc<RwLock<HashSet<String>>>` accessed directly. Wrapped in a `dead_nodes() -> Vec<String>` accessor on `ClusterState` that sorts the output — `/cluster/status` should be deterministic across scrapes for diff-friendliness in operator workflows.
- Brief's `test_query_routes_to_replica_after_failover` was sketched at the server level. Implemented at the ring level instead — it's the correct layer for the invariant ("removing a node from the ring routes that key to the next walker"), and a server-level test would require spinning up multiple AppStates with a real cluster handle, which the same sweet of integration tests already covers via `tests/cluster_integration.sh`.
- Brief suggested incrementing `ring_changes_total` once per add/remove batch ("on every add or remove"). Implemented as one increment per individual add or remove (a 2-add reconcile bumps the metric by 2). Reads better in dashboards because each event is one tick on the counter.

**Open / next:**
- **Anti-entropy / read repair (M23) is the natural follow-up.** Re-joined nodes serve stale data — the entries inserted while they were dead live only on the survivors. M23 should add either: (a) a background gossip-driven anti-entropy pass that compares Merkle-tree-style digests between replica pairs and ships the missing entries, or (b) read-repair on every cache miss (issue a parallel query to all replicas, take any hit, write back the result to misses). Read-repair is simpler and fits the cache semantics — for a portfolio implementation, that's likely the right call.
- **Hinted handoff isn't implemented.** When inserts happen while a replica is Dead, that replica simply doesn't get the write. When it comes back, the missing entries stay missing. Hinted handoff (the coordinator stores intended writes for the dead replica in a "hint log" and replays them on re-join) is a known Cassandra pattern. For ferrocache it's overkill — cache misses are cheap.
- **Detector entry growth in long-running churn.** A node that joins, dies, re-joins, dies, re-joins... accumulates a single detector entry whose history reflects all those gaps. The window is bounded at 100 samples by `phi_window_size`, but if a peer is permanently removed (operator action, not reflected in any current code path), its detector entry sticks around. A future `ClusterState::evict_peer()` API + a reconciler signal to the detector would close this. Out of scope for M22.
- **Race between M21 soft-skip and M22 ring-removal.** Both compete for the "skip the dead peer" responsibility; both are fine in isolation. The current behavior under race (e.g. peer is in both ring and `peer_status==Dead` for one tick): M21's soft skip fires first in the handler (returns 200 miss), so the M22 path doesn't get to run for that request. After the next reconcile tick, the ring removes the peer and M22 takes over. This is fine — both paths produce the same observable outcome (200 not 502).
- **Docker integration test sleep is 35s.** That's 35s × 2 (kill + restart) = 70s of new test runtime. CI gets slower. A dedicated "fast failover" config (lower phi threshold or chitchat dead-time) would let us tighten this, but every config knob lowered is a false-positive risk on a real production deployment. Acceptable cost for the assertion fidelity.
- **`peer_health` keeps the dead peer's entry forever.** Even after the peer is removed from the ring, its `phi=307` and `status=dead` entry stays in `peer_health` — useful for "I want to see what's been dying", less useful when an operator scrolls through. Pruning entries that have been dead for >1 hour would be a reasonable extension; not implemented.
