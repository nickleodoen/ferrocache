# ferrocache — Project Context

## Section 1: Evergreen

**What this is:** Distributed semantic cache for LLM applications, written in Rust. Single binary, multi-node via consistent hashing + gossip replication. Portfolio project for big-tech Core SWE interviews.

**Current phase:** All phases complete (M1–M19). ferrocache is production-ready and secure.

**Completed work:**
- M1: axum scaffold (3 routes, tracing, env-based port)
- M2: hnsw_rs integration (DistCosine, side-table keyed by usize, dim lock)
- M3: WAL (NDJSON, fsync-per-insert, replay on startup, corrupt-line skip; WAL-first insert path; UUIDs stable across restarts)
- M4: config crate (TOML + env merge), request validation (4096 dim, 100KB resp, threshold range), /stats endpoint, dead-code cleanup
- M5: consistent hash ring (FNV-1a, 64 vnodes default) + chitchat gossip discovery, /cluster/status endpoint, single-node fallback
- M6: cluster-aware /query routing + /insert synchronous replication via reqwest, `?local=true` loop-prevention, coordinator UUID stamping, get_n_nodes replica walk
- M7: Dockerfile + docker-compose 3-node cluster + bash integration tests; fixed env-var `prefix_separator` config bug (latent since M4)
- M8: README + Mermaid diagram, criterion benchmarks (real numbers), stdlib-only Python client, GitHub Actions CI (check + integration), library/binary split for bench imports
- M9: simulation harness (`tests/simulate.py` + `simulate_no_ml.py`) — FAQ workload with sentence-transformers, 100% hit rate / 0 false positives at 0.90 threshold
- M10: drop-in SDK middleware (`wrap_openai`, `wrap_anthropic`) with proxy attribute delegation, fail-open on cache outage, env-var config; restructured Python client into a package
- M11: framework backends — `FerrocacheCache` (LangChain `BaseCache`) + `FerrocacheLLM` (LlamaIndex `CustomLLM` subclass); optional imports, fail-open default, custom `embed_fn` supported
- M12: MCP server (`ferrocache.mcp_server`) over stdio — 3 tools (`semantic_cache_lookup`, `semantic_cache_store`, `cache_status`), text-in/JSON-out (embedding handled internally), Claude Desktop + Claude Code setup docs
- M13: distribution — `pyproject.toml` with optional extras (`[openai]`, `[anthropic]`, `[langchain]`, `[llamaindex]`, `[mcp]`, `[all]`); base `pip install ferrocache` is zero-deps; root + Python LICENSE; release CI on `v*` tags publishes Docker image to GHCR and Python package to PyPI via OIDC trusted publishing
- M14: namespace partitioning — `SemanticIndex` is a `HashMap<model_id → NamespacedIndex>`, each with its own HNSW + side-table; `model_id` required on `/insert` and `/query`; Python integrations auto-derive `model_id` from default embed model; PyPI bumped to 0.2.0; pre-M14 WAL entries migrate to `legacy::unknown`
- M15: WAL compaction + snapshotting — `src/snapshot.rs` writes `magic+version+wal_seq+bincode(Vec<SnapshotEntry>)` atomically; WAL gained monotonic `sequence` numbers; startup loads snapshot then replays only `seq > watermark`; auto-compaction every 10K inserts (configurable) + manual `POST /admin/compact`; `CacheEntry` re-stores `embedding`+`query_text`
- M16: Prometheus `/metrics` (hand-written text exposition, no `prometheus` crate) — atomic counters with per-namespace `RwLock<HashMap>`, fixed 16-bucket `LatencyHistogram` (100µs → 10s), `/stats` JSON gains `counters` block, monitoring compose overlay (Prometheus + Grafana on :9090/:3100) with provisioned datasource and 8-panel ferrocache dashboard
- M17: bearer token auth — opt-in via `FERROCACHE_AUTH_TOKEN`; `subtle::ConstantTimeEq` compare; `/health` and `/metrics` exempt; inter-node forwards carry the same token; Python client reads env automatically; integrations gain `auth_token=` kwarg
- M18: cluster mTLS — opt-in via `FERROCACHE_CLUSTER__TLS__ENABLED`; rcgen 0.13 + rustls 0.23; second TLS listener on `internal_port` (default `port + 1000`) requires client cert chained to cluster CA; `WebPkiClientVerifier` rejects anonymous clients at handshake; `gen_certs` binary writes dev CA + per-node cert/key; production loads PEMs from `cluster.tls.{ca_cert,node_cert,node_key}_path`

**Module map:**
- lib.rs — re-exports modules so benches and external consumers can import
- main.rs — entry, tracing init, config load, WAL replay, optional cluster init, serve
- server.rs — router, handlers, request validation, routing/replication coordination, tests
- index.rs — SemanticIndex (hnsw_rs wrapper + HashMap side-table), replay_entry
- wal.rs — Wal (append w/ fsync, mkdir -p parent), WalEntry, replay
- models.rs — request/response DTOs (InsertRequest carries optional `uuid`)
- config.rs — FerrocacheConfig, HnswConfig, ClusterConfig (gossip_addr, api_addr, replication_factor, seed_nodes)
- state.rs — AppState { node_id, index, wal, wal_path, snapshot_path, hnsw_config, cluster, router, replication_factor, compact_interval_inserts, inserts_since_compact, metrics, auth_token, max_replication_retries }
- auth.rs — `auth_middleware` (axum `from_fn_with_state`); constant-time `subtle::ConstantTimeEq`; `/health` + `/metrics` exempt; only installed when `auth_token` is non-empty
- tls.rs — `TlsBundle`, `generate_ca`/`generate_node_cert` (rcgen 0.13), `load_or_generate` (disk vs in-memory dev), `build_server_config` (rustls 0.23 mTLS — `WebPkiClientVerifier` requires a client cert chained to the cluster CA), `install_default_crypto_provider` (idempotent aws-lc-rs install); used by `main.rs` for the internal listener and `router.rs` for the reqwest identity
- bin/gen_certs.rs — dev-cert generator binary (`cargo run --bin gen_certs [node_ids...]`); writes `./certs/ca.pem` + `./certs/<node_id>/{cert,key}.pem`; intentionally not built into the production Docker image
- ring.rs — HashRing (BTreeMap<u64, node_id>, FNV-1a, virtual nodes, get_n_nodes for replica walk)
- cluster.rs — ClusterState wrapping chitchat; reconciler syncs ring + node_id→api_addr map from gossip KV
- router.rs — ClusterRouter (reqwest client) — forward_query / forward_insert return typed `reqwest::Result` so the `forward_with_retry` helper can classify failures (retry on connect errors / timeouts / 5xx; never on 4xx); ±20% jitter from `rand::thread_rng()`; metric `ferrocache_replication_retries_total` increments inside the helper
- snapshot.rs — `SnapshotEntry` + `write_snapshot`/`read_snapshot` (magic+version+wal_seq+bincode); `compact()` helper that writes the snapshot and truncates the WAL atomically
- metrics.rs — `Metrics` (atomic counters + per-namespace `RwLock<HashMap<NamespaceMetrics>>`), `LatencyHistogram` (16 fixed buckets 100µs–10s), `render()` to Prometheus text exposition format
- benches/cache_bench.rs — criterion: insert / query_hit / query_miss / insert_with_wal / snapshot_write_10k
- clients/python/ferrocache.py — stdlib-only Python client (urllib + json)

**Architecture decisions (append-only, one line each):**
- Client computes embeddings externally; ferrocache stores/compares f32 vectors
- Crates: tokio, axum 0.7, hnsw_rs, chitchat 0.10, tracing, serde_json, config, reqwest
- WAL format: newline-delimited JSON, replayed on startup, fsync per insert
- Consistent hashing key: u64 from first 8 bytes of embedding (first 2 f32 values, big-endian)
- Cosine distance: hnsw_rs DistCosine returns 1-similarity; we convert
- Side-table: HashMap<usize, CacheEntry> keyed by hnsw internal id; UUID inside CacheEntry
- WAL is source of truth; production insert path is WAL-first then replay_entry
- insert() (UUID-generating) is #[cfg(test)] only; production uses replay_entry
- Config priority: env vars (FERROCACHE_ prefix, `_` prefix-sep, `__` section-sep, `,` list-sep) > ferrocache.toml > defaults
- tokio::sync::RwLock for index, tokio::sync::Mutex for WAL
- Validation limits (server.rs constants): MAX_EMBEDDING_DIM=4096, MAX_RESPONSE_BYTES=102_400
- Threshold accepted range on /query: 0.0..=1.0
- Hash ring: FNV-1a (cross-process deterministic), 64 virtual nodes per physical node by default
- Cluster mode is opt-in via `cluster.enabled`; `false` keeps Phase 1 behavior bit-identical
- Ring reconciler runs every 2s in a background task; chitchat gossip interval = 1s
- Each node advertises its API addr via chitchat KV under key `api_addr`; reconciler reads peers' addrs
- Routing: `?local=true` skips ring lookup (used by forwarded requests + debugging); coordinator stamps UUID before fan-out
- Replication: synchronous, primary-or-coordinator + (replication_factor-1) replicas; any replica failure → 502
- Forward failures return 502 (BAD_GATEWAY) to distinguish "peer failed" from local 500
- Docker runtime image needs `libgomp1` (hnsw_rs → rayon → OpenMP)
- `seed_nodes` is parsed from a comma-separated env string via config-rs `list_separator(",")`+`with_list_parse_key`
- `Wal::open` mkdir's parent dir so containers can mount an empty `/data` volume
- Crate is library + binary (lib.rs re-exports modules); `cargo build --release --bin ferrocache` for prod images
- Benchmarks via criterion; Python client uses only stdlib (urllib+json)
- Python package on PyPI with optional extras; Docker image on GHCR; release CI on version tags
- Index is namespace-partitioned by `model_id`; each namespace has its own HNSW instance + side-table
- `model_id` is required on `/insert` and `/query`; old WAL entries without it default to `legacy::unknown`
- `model_id` format convention: `model_name::dimension` (e.g. `all-MiniLM-L6-v2::384`)
- Cross-namespace queries are impossible by construction — vectors from different models never compare
- Snapshot format: `magic (FERROSNA) + version (u64=1) + wal_sequence (u64) + entry_count (u64) + bincode-encoded Vec<SnapshotEntry>`; written atomically via temp+rename so a crash never corrupts the prior snapshot
- Compaction: snapshot the side-table entries, then truncate the WAL — HNSW is rebuilt from stored embeddings on load (no need to serialize the graph itself)
- WAL entries carry a monotonic `sequence: u64` that never resets — startup skips entries `<= snapshot_watermark` so the WAL tail replay is bounded
- Auto-compaction every N inserts (default 10K via `compact_interval_inserts`, 0 disables); manual via `POST /admin/compact`
- `CacheEntry` again stores `embedding` + `query_text` so snapshots can dump the full record (re-added in M15; was dropped in M4)
- Prometheus `/metrics` is hand-written (no `prometheus`/`metrics` crate); atomic counters with `Ordering::Relaxed` + a fixed-bucket `LatencyHistogram`
- Per-namespace hit/miss/insert counters carry a `namespace` label; global query/insert latency histograms have 16 buckets (100µs → 10s) plus a `+Inf` bucket
- `/stats` JSON gained a `counters` object so users without Prometheus can still read the same numbers
- Monitoring stack lives in `monitoring/` as a docker-compose overlay (`-f docker-compose.yml -f monitoring/docker-compose.monitoring.yml`); base cluster runs unchanged without it
- `CacheEntry` again stores `embedding` + `query_text` so snapshots can dump the full record (re-added in M15; was dropped in M4)
- Bearer token auth via `FERROCACHE_AUTH_TOKEN` env var; disabled when unset (opt-in, backward compatible)
- Auth exempts `/health` and `/metrics` (load balancers and Prometheus need unauthenticated access)
- Token comparison uses `subtle::ConstantTimeEq` to prevent timing attacks; the token value is never logged at any level
- Inter-node replication forwards include the same bearer token (shared-secret cluster model — all nodes carry the same `FERROCACHE_AUTH_TOKEN`)
- mTLS between cluster nodes via rustls 0.23 + rcgen 0.13; opt-in via `cluster.tls.enabled`; second TLS listener on `internal_port` (default `port + 1000`) serves the same router; public port stays plain HTTP
- Dev mode (no cert paths) auto-generates a self-signed CA + leaf in memory; production loads PEMs from `cluster.tls.{ca_cert,node_cert,node_key}_path`
- TLS reqwest client trusts ONLY the cluster CA (system roots disabled) and presents the node leaf as client identity — i.e. mTLS, not just TLS
- Gossip (chitchat UDP) remains unencrypted: it carries only ring metadata (node IDs + forwarding addresses), not cached data
- `cargo run --bin gen_certs [node_ids...]` writes `./certs/ca.pem` + `./certs/<node>/{cert,key}.pem` for local clusters; the production Dockerfile builds only `--bin ferrocache`, so `gen_certs` never ships in images
- Replication retry: exponential backoff (50ms × 2^attempt, ±20% jitter, capped at 5s), max 3 attempts by default; retries on connect errors + timeouts + 5xx only, never on 4xx (deterministic failures)
- `ferrocache_replication_retries_total` metric tracks retry pressure separately from final failures — retries that succeed do NOT count as failures, so dashboards can distinguish "noisy network, eventually succeeded" from "peer permanently down"

**Non-negotiable constraints:**
- Auth (bearer token) and cluster mTLS are both opt-in; public-port TLS handled by reverse proxy
- No UI
- No direct OpenAI/Anthropic API calls inside ferrocache
- Use tokio, not async-std
- Every new module gets unit tests before moving on

**Session log rules:**
- Keep only the last 2 session logs below
- When adding a new log, delete the oldest if there are already 2
- Summarize deleted sessions as one-line entries under "Completed work" above

## Section 2: Rolling Session Log (last 2 sessions only)

### 2026-05-02 — Mission 18: mTLS Between Cluster Nodes
**Built:** New `src/tls.rs` (added to `lib.rs`) — `TlsBundle { ca_cert_pem, node_cert_pem, node_key_pem }`, `generate_ca()` (rcgen 0.13, `IsCa::Ca(Unconstrained)` + `KeyCertSign|CrlSign` usage), `generate_node_cert(ca, ca_key, node_id, sans)` (each SAN string is auto-classified as `IpAddr` or `DnsName` by rcgen's `CertificateParams::new`; both `ServerAuth` and `ClientAuth` EKUs are set so the same cert plays both roles in mTLS), `load_or_generate(tls_config, node_id, api_addr)` (all three paths set → load from disk; any missing → in-memory dev mode + warning), `pem_to_der` helper (rustls-pemfile → `Vec<CertificateDer> + PrivateKeyDer::Pkcs8`), `build_server_config(bundle) -> rustls::ServerConfig` (`WebPkiClientVerifier::builder(roots).build()` for *required* client auth — anonymous clients are rejected at the handshake, not just downgraded), `install_default_crypto_provider()` (idempotent `aws-lc-rs::default_provider().install_default()`). `Cargo.toml` switched `reqwest` to `default-features = false, features = ["json", "rustls-tls"]` so the same `rustls 0.23.40` resolves across `reqwest` / `axum-server` / standalone — verified via `cargo tree -i rustls`. New deps: `rcgen 0.13`, `rustls 0.23`, `rustls-pemfile 2`, `tokio-rustls 0.26`, `axum-server 0.7 (features = ["tls-rustls"])`. `ClusterTlsConfig { enabled, ca_cert_path: Option<String>, node_cert_path: Option<String>, node_key_path: Option<String>, internal_port: Option<u16> }` added to `ClusterConfig` under `pub tls`; all four fields use `#[serde(default)]` so env vars work without builder defaults (same precedent as `node_id` and `auth_token`). `ClusterRouter::new` now takes `(auth_token, tls_bundle: Option<&TlsBundle>) -> Result<Self>`; when bundle present, builds a reqwest client with `tls_built_in_root_certs(false)` + `add_root_certificate(ca)` + `identity(node_cert+key)`; `forward_query` and `forward_insert` use `https://` vs `http://` based on a `tls_enabled` flag captured at construction. `ClusterState::new` gained a `forward_addr: &str` parameter — the addr peers should dial for replication — which becomes the value chitchat advertises under the existing `api_addr` KV key (homogeneous cluster: every node uses the same scheme/port, so one KV is enough). `main.rs` flow: install crypto provider → `tls::load_or_generate` (gated on `cluster.enabled && cluster.tls.enabled`; warns + ignores if `cluster.enabled=false`) → derive `internal_port = config.tls.internal_port.unwrap_or(port + 1000)` → derive `internal_addr` from the host part of `api_addr` → pick `forward_addr = internal_addr if tls else api_addr` → build `ClusterRouter::new(token, bundle.as_ref())?`. Listener loop: when bundle present, serve `app.clone()` on the public TCP listener AND `axum_server::bind_rustls(internal_socket, RustlsConfig::from_config(server_config))` on the internal port; `tokio::select!` over the two futures so either failing tears the process down. New binary `src/bin/gen_certs.rs` (cargo auto-discovers `src/bin/*` as `[[bin]]` targets — no Cargo.toml edit needed) writes `./certs/ca.pem` + `./certs/<node_id>/{cert,key}.pem` for an arbitrary node-id list (defaults to `node1 node2 node3`); SANs `[<node_id>, localhost, 127.0.0.1]`. The Dockerfile already builds only `--bin ferrocache`, so `gen_certs` never ships. Logging: `tls::load_or_generate` logs paths but never PEM contents; `main.rs` logs `cluster mTLS enabled` + `ferrocache TLS listening (cluster) addr=…` and a warning when the derived `internal_port` collides (numerically — different proto) with the gossip UDP port number. Docs: `ferrocache.toml` and `docker-compose.yml` (per-node) gained commented `[cluster.tls]` blocks pointing at `/certs/<node>/{cert,key}.pem`; the compose file also has a commented `./certs:/certs:ro` volume mount and a comment recipe (`cargo run --bin gen_certs node1 node2 node3` → mount → uncomment). 7 new tls-module tests + the existing 88 unchanged.

**Verified:**
- `cargo build` (debug + `--release --bin ferrocache --bin gen_certs`) — clean.
- `cargo test` — 95/95 pass (was 88; +7 tls module tests including the in-process mTLS roundtrip and the bad-CA rejection — both spin up `axum_server::bind_rustls` against `RustlsConfig::from_config(...)` and drive a real TLS handshake via reqwest, no docker required).
- `cargo clippy --all-targets -- -D warnings` — clean (after fixing one `doc_lazy_continuation` lint in `ClusterTlsConfig`'s docstring).
- `cargo fmt --check` — clean.
- `python3 -m unittest discover tests` — 39/39 pass (no Python changes needed; integrations talk to the public port over plain HTTP + bearer token).
- Live mTLS smoke test on the release binary (single-node, gossip listening but no peers): generated certs with `gen_certs`, started node with `FERROCACHE_CLUSTER__TLS__ENABLED=true` and the three cert paths → public port `:3997` plain HTTP `/health` → 200 → internal port `:4997` plain HTTP → handshake refused (curl reports `HTTP/0.9 when not allowed`) → internal port HTTPS without client cert → handshake refused with TLS error 1116 (`no_certificate`, exactly what mTLS demands) → internal port HTTPS with the proper client cert → `{"status":"ok","node_id":"n1",…}` 200. Confirmed via `grep` that no PEM material lands in logs.
- `docker compose up -d --build` (TLS off, default) → `tests/cluster_integration.sh` → 20/20 pass — backward compat preserved.

**Key decisions:**
- Single rustls version across the dep tree. The brief flagged this as risk #1; resolved by switching reqwest to `default-features = false, features = ["json", "rustls-tls"]`. `cargo tree -i rustls` confirms one `rustls v0.23.40` shared by `reqwest`, `axum-server`, `tokio-rustls`, and the direct `rustls` use-site. Using rustls's default crypto-provider (`aws-lc-rs`) means we get FIPS-validated crypto for free with no behavior change.
- Separate internal port for TLS, not SNI/ALPN multiplexing on the public port. The brief's reasoning holds: external clients don't carry client certs (load balancers, Prometheus, Python SDKs), and detecting TLS via SNI inspection adds 200-ish lines of low-value code. The "second listener on +1000" pattern matches Envoy/Linkerd sidecars and is what an operator already expects.
- chitchat KV key stays `api_addr` regardless of TLS state. The brief proposed advertising both `api_addr` and `internal_addr`, but in a homogeneous cluster (per the brief's own constraint: all nodes share the same TLS config) every peer wants the same forwarding addr. Adding a second key buys nothing and creates a "what if they disagree?" failure mode. So `api_addr` semantically becomes "addr peers use to reach me for replication", and TLS-enabled nodes advertise `host:internal_port` under that key; TLS-disabled nodes advertise `host:public_port` as before. Documented in the `ClusterState::new` docstring.
- rcgen 0.13 sign flow: `params.signed_by(&new_keypair, &issuer_cert, &issuer_key)`. The first arg is the *new cert's* keypair (which `impl PublicKeyData`), not the issuer's — easy to get backwards. Smoke-tested by writing `test_generate_node_cert_distinct_from_ca` and `test_build_server_config_smoke` early so this would surface immediately.
- mTLS uses `WebPkiClientVerifier::builder(roots).build()` (not `.allow_unauthenticated()`). Anonymous clients are rejected during the handshake — the auth happens before any HTTP framing, so even `/health` on the internal port requires a client cert. That's intentional: the public port already serves unauthenticated `/health`, and a "TLS-but-anonymous-OK" path on the internal port would be a footgun where someone aims a load-balancer at it and accidentally exposes cluster traffic.
- Crypto provider install is idempotent + deterministic. Brief didn't specify when to install it; chose the latest possible moment (just before `load_or_generate`) and made the call `let _ =` so re-installs are silent. This keeps test ordering simple — every tls test that needs the provider calls `install_default_crypto_provider()` first, and concurrent tests don't race.
- Reqwest client's `tls_built_in_root_certs(false)` is essential. Without it, a peer cert signed by *any* system-trusted CA (Let's Encrypt, the OS root store, anything) would be accepted. With it, only certs signed by the cluster CA verify — which is the entire point of having a private cluster CA.
- The `gen_certs` binary lives at `src/bin/gen_certs.rs` so cargo auto-discovers it; no `[[bin]]` block needed (the brief suggested adding one, but auto-discovery is the idiomatic 2024-edition pattern). The Dockerfile builds only `--bin ferrocache`, so the cert generator never ships in production images — verified by inspection of the Dockerfile's `RUN` line.
- Re-using `app.clone()` for the second listener works because `axum::Router` is `Clone` and shares state via `Arc` internally. So `/metrics` over the internal port reflects the same counters as `/metrics` over the public port — there's exactly one `Metrics` struct in memory, not one-per-listener.

**Deviations:**
- Brief proposed advertising both `api_addr` and `internal_addr` as separate chitchat KVs. Used a single `api_addr` key whose semantic shifts based on local TLS state. Rationale above; documented in code.
- Brief said `subject_alt_names: impl Into<Vec<String>>` on `CertificateParams::new` — confirmed by reading the source (`CertificateParams::new` parses each entry as `IpAddr::from_str` first, falling back to `DnsName`). No need for a custom SAN-builder helper as a result; passing `["node1", "127.0.0.1"]` produces one DnsName + one IpAddress SAN automatically.
- Brief showed `[[bin]] name = "gen_certs"` in Cargo.toml. Skipped — `src/bin/*.rs` is auto-discovered. `cargo run --bin gen_certs` works without the explicit declaration.
- Brief showed an example `KeyPair::generate_for(...)` style. Used the simpler `KeyPair::generate()` (rcgen's default — ECDSA P-256). Both work; the simpler form gives smaller PEM output and is what every rcgen example uses.
- Brief's example `test_node_cert_has_san` parses the cert and verifies SAN contents. Replaced with `test_build_server_config_smoke` + the in-process roundtrip — the roundtrip *implicitly* verifies SANs work because the client connects to `127.0.0.1` and rustls validates the cert's SAN list against the dial-name. A successful handshake = SANs are correct. Lighter than parsing X.509 by hand.
- Brief's compose-file note about commented TLS env vars used `INTERNAL_PORT: "4443"`. Followed exactly, but added a runtime warning (in `main.rs`) when the derived `internal_port` numerically equals the gossip UDP port — they're independent protocols, so it works, but reading "port 4000" in two places in a firewall rule is a footgun.
- Brief asked for two test fixture pairs (`test_mtls_replication_roundtrip`, `test_mtls_rejects_unknown_ca`) plus several smaller ones. Implemented exactly that — both end-to-end tests run an `axum_server::from_tcp_rustls` server in a `tokio::spawn` against an ephemeral 127.0.0.1 port and drive it with reqwest. Roundtrip → 200; bad CA → handshake error.
- Brief proposed `axum_server::bind_rustls(addr, RustlsConfig::from_config(...))` for production. Used exactly that in `main.rs`. Used `from_tcp_rustls(std_listener, ...)` in the tests because they need to capture the OS-assigned ephemeral port before serving (`bind_rustls` doesn't expose the bound socket). Both code paths exercise the same mTLS handshake.

**Next session (M19 if pursued):** Three small follow-ups bundled. (a) Replication retry — the cluster fan-out today returns 502 on the first peer failure. A 2-attempt retry with 50ms backoff would handle transient connect/handshake errors without changing the visible API. (b) `docs/security.md` — consolidating M17 + M18 into one "running ferrocache securely" doc with a threat-model section, the certificate-rotation runbook, and an explicit "what is NOT protected" list (gossip, public port, at-rest data). (c) A `.claude/skills/ferrocache.md` skill file so future Claude sessions can answer common ferrocache operator questions without re-reading the whole repo.

**Open:** Cluster-mode mTLS integration test against actual Docker is missing — the in-process tests cover the rustls handshake and the integration script covers TLS-off; "TLS-on across 3 docker containers via gossip discovery" is the gap. Closing it requires a `docker-compose.tls.yml` overlay that mounts the certs directory and sets the env vars. Skipped for the same reason auth-on integration was skipped in M17: a compose-overlay + parallel script doubles the integration-test maintenance surface and the in-process tests already verify the mTLS handshake end-to-end. The `gen_certs` certs have multi-millennium validity (rcgen's default `not_after = 4096-01-01`) — fine for dev, but `docs/security.md` should call out that production deployments need a real cert lifecycle (CA rotation, leaf rotation, OCSP/CRL distribution) which ferrocache doesn't help with. The internal listener has no graceful-shutdown integration with `axum_server` — `tokio::select!` aborts the spawn task when the public listener exits, but in-flight TLS connections aren't drained. axum-server has a `Handle::shutdown()` API; wiring it through is a nice-to-have. There's no per-cert revocation (CRL/OCSP) — `WebPkiClientVerifier::builder` accepts CRLs, but ferrocache doesn't surface them through config; if a node's key leaks, the only mitigation is rotating the entire CA. The choice of `aws-lc-rs` over `ring` was implicit (rustls's default-features picked it). Both are fine for portfolio purposes; production deployments concerned about FIPS would explicitly opt into `aws-lc-rs`'s FIPS feature, which is out of scope here.

### 2026-05-02 — Mission 19: Retry Logic + Security Audit + Docs (Phase 5 close-out)
**Built:** Three concerns in one mission. (1) **Replication retry** — `rand = "0.8"` added to `Cargo.toml`; `ClusterConfig` gained `max_replication_retries: usize` (default 3, env `FERROCACHE_CLUSTER__MAX_REPLICATION_RETRIES`); `AppState` gained the same field plumbed through `AppState::new`; `Metrics` gained `replication_retries_total: AtomicU64` + `record_replication_retry()` + a new `# HELP/# TYPE` block in `render()` (`ferrocache_replication_retries_total`); `CountersResponse` (the `/stats` JSON `counters` block) gained `replication_retries: u64`. The retry pattern itself: `forward_query` / `forward_insert` were refactored to return `reqwest::Result<T>` (instead of anyhow) so the new helper can classify errors structurally — `error_for_status()?` ensures 5xx surfaces as a `reqwest::Error` with `.status()` set. New `pub async fn forward_with_retry<F, Fut, T>(max_retries, &Metrics, mut make_request: F) -> Result<T> where Fut: Future<Output = reqwest::Result<T>>` runs the loop: `total = max_retries.saturating_add(1)` attempts, on each retry calls `metrics.record_replication_retry()` then sleeps `compute_retry_delay(attempt)` (50 ms × 2^(attempt-1) base, ±20% jitter via `rand::thread_rng().gen_range(-jitter..=jitter)`, capped at 5 s so a misconfigured `max_retries` can't park indefinitely). `is_retryable(&reqwest::Error)` returns true on `is_connect()` ‖ `is_timeout()` ‖ `status().is_some_and(|s| s.is_server_error())`; **everything else (including 4xx) returns immediately**. `server.rs`'s two fan-out sites (`forward_query` for `/query` routing, the cluster fan-out loop in `insert_handler` for `/insert` replication) both now go through `router.forward_with_retry(state.max_replication_retries, &state.metrics, || router.forward_*(addr, req))`; metric `record_replication_failure()` only fires after retries are exhausted. (2) **Security audit** — three findings, all fixed. `bad_gateway` callers used to embed peer node IDs and the underlying reqwest error in the response body (e.g. `"failed to reach node node2: connection refused"`); now the body is a constant `"upstream replica unavailable"` and the peer ID + error are logged via `tracing::error!(peer = %target_id, error = %e, ...)`. The WAL-append failure path used to format `"WAL append failed: {e}"` into the 500 body, where the anyhow chain (built via `with_context(|| format!("failed to open WAL at {path}"))`) carried the on-disk WAL path; body is now `"persistence failed"`, the chain still goes to logs. The `/admin/compact` failure path had the same shape (snapshot/WAL paths leaking through the chain); body is now `"compaction failed"`. The `index.replay_entry` 500 ("dimension mismatch: expected X, got Y") was kept as-is — controlled message, no path/ID, genuine validation feedback the test asserts on. Audit also confirmed clean: zero log calls embed the auth token value (only the literal string `"FERROCACHE_AUTH_TOKEN not set"`); `Authorization` header value is never logged; M17's field-by-field config tracing prevents Debug-derived token leaks; no panics reach an HTTP body; TLS load logs paths but never PEM contents; `unauthorized()` body never echoes the provided token. (3) **Docs** — new `docs/security.md` (~140 lines) with 4-row attack-surface threat-model table (public API, cluster traffic, filesystem, gossip UDP), three deployment recipes (single-node dev, single-node behind reverse proxy, multi-node cluster with mTLS), certificate management guidance, firewall port table, configuration reference, and an explicit "Known Limitations" list (no at-rest encryption, no per-client ACLs, no CRL/OCSP, no rate limiting, no token rotation hot-reload, gossip UDP unencrypted, tokens not zeroized in memory, public-port TLS out of scope). README's M17-era "Security" section condensed and pointed at `docs/security.md`. New `clients/ferrocache-skill.md` (~280 lines) — LLM-targeted reference: full API schemas with curl examples (every snippet uses `model_id` + `Authorization: Bearer`), six integration patterns (Python client / OpenAI middleware / Anthropic / LangChain / LlamaIndex / MCP) with copy-pasteable snippets that all carry `auth_token=`, threshold-selection table, namespace rules, production checklist with metric-driven alert recommendations, and a 7-entry troubleshooting section. `ferrocache.toml` gained a commented `max_replication_retries = 3` line. 4 new retry tests added in `router.rs`: `test_retry_on_connection_error` (mock fails 2× then succeeds; assert `count == 3` AND `replication_retries_total == 2`), `test_no_retry_on_4xx` (mock returns 400; assert `count == 1` AND `replication_retries_total == 0`), `test_retry_exhausted_returns_error` (mock always 500; with `max_retries = 2` assert `count == 3` AND `replication_retries_total == 2`), `test_retry_counter_increments` (locks in metric increment shape).

**Verified:**
- `cargo test` — 99/99 pass (was 95; +4 retry tests).
- `cargo clippy --all-targets -- -D warnings` — clean.
- `cargo fmt --check` — clean.
- `python3 -m unittest discover tests` — 39/39 pass (no Python changes).
- `docker compose up -d --build && ./tests/cluster_integration.sh && docker compose down -v` — 20/20 pass; backward compat preserved with retry/audit changes.
- Audit confirmed no auth tokens, internal paths, node IDs, or stack traces appear in any HTTP error body. Logs retain the detail (peer IDs, paths, anyhow chains) so on-call still has what they need.

**Key decisions:**
- Retry helper is a *closure-driven* combinator (`make_request: FnMut() -> Fut`), not an embedded retry inside `forward_query`/`forward_insert`. Reasoning: the closure pattern is the brief's ask, and it lets us keep `forward_*` as single-attempt primitives that tests can drive directly. The downside (slight syntactic overhead at call sites in server.rs) is small.
- `forward_query`/`forward_insert` return `reqwest::Result<T>` instead of anyhow. The retry classifier needs the typed error; converting back to anyhow would lose `is_connect()`/`is_timeout()`/`status()`. The boundary back to anyhow happens inside `forward_with_retry` so the public callers (server.rs) still get an anyhow `Result<T>`.
- `error_for_status()?` is the cleanest way to make a 5xx response surface as a `reqwest::Error` with `.status()` set. Without it, 5xx returns `Ok(resp)` and you have to manually classify; with it, the retry classifier's `e.status().is_some_and(|s| s.is_server_error())` path Just Works.
- Backoff is capped at 5 s. With `max_retries = 3` the natural sequence is 50 ms / 100 ms / 200 ms — well under the cap. The cap exists to defend against a config typo (`max_retries = 50` would otherwise schedule a 28-hour wait on the last retry).
- Jitter is **±20%** symmetric, not full jitter (0..base). Symmetric jitter keeps the expected delay equal to the nominal exponential schedule, which is what an operator reading the docs expects. Full jitter would be theoretically better at reducing thundering herd but harder to reason about. Per replica per cycle, the variance is enough.
- `is_retryable` excludes 4xx by design. 401 (wrong token) won't be fixed by retrying. 400 (bad dimension) won't either. The only retryable HTTP class is 5xx (the peer is having a moment); everything else is the caller's mistake. Codifying this in `is_retryable` rather than at the call site keeps the policy in one place.
- `replication_retries_total` is its own counter, not derived from `replication_failures_total - successful_calls`. Reasoning: a retry that eventually succeeds should NOT increment failures. So an operator looking at `failures_total` sees actual unrecoverable peer issues; looking at `retries_total` sees noise. Two metrics, two distinct alerting policies.
- Audit fix: scrub error *bodies*, not *logs*. The brief explicitly carved this out — paths, peer IDs, and underlying error chains in logs are fine (operators need them to debug). The leak is when this material flows through the HTTP error body to an attacker who can poke `/insert` or `/query`. Three offending sites all routed through anyhow's `format!("...{e}")`; replacing the format string with a constant string is a one-line fix per site.
- `forward_query` retry was added too, even though the brief was less emphatic about it (replica-failure 502 was the main concern). Same helper, same policy — a flaky peer mid-route is just as worth retrying as a flaky peer mid-replicate.

**Deviations:**
- Brief showed `e.status().map_or(false, |s| s.is_server_error())`. Rust 1.82+ ships `Option::is_some_and` which reads cleaner; used that. Behavior identical.
- Brief showed `rand::random::<u64>() % (jitter_range * 2) - jitter_range`. That's modulo bias plus an underflow on a u64 subtraction. Replaced with `rand::thread_rng().gen_range(-jitter_ms..=jitter_ms)` — uniform, signed, no panic risk. Same effect.
- Brief mentioned "Implement retry in `router.rs` … wrap forward_insert and forward_query in a retry loop". Took the closure-combinator interpretation (`forward_with_retry(closure)`), which matches the brief's wiring example more faithfully than embedding retry inside the forward methods themselves.
- Brief specified `max_replication_retries = 3` as the default. Followed. Wired both via `#[serde(default)]` on the field AND a `set_default("cluster.max_replication_retries", 3)` in the config builder so env-var overrides round-trip cleanly.
- Audit found the bad_gateway / WAL-append / compaction leaks the brief flagged as "should not appear in error responses from /query, /insert, etc.". Fixed all three. The brief said "If any leaks are found, fix them. If all paths are clean, document that you verified this." — both happened; the rest of the audit was clean.
- Skill file's `clients/ferrocache-skill.md` placement: brief said `clients/ferrocache-skill.md`. Followed exactly. Consideration: `.claude/skills/ferrocache.md` would be the convention for a Claude Code project skill, but the brief's path treats this as a public artifact (alongside `clients/python/`), which makes sense — it's a doc for *any* LLM, not a private Claude Code skill.

**Open / next:**
- mTLS-on integration testing against the docker compose cluster is still missing (carried forward from M18). Closing it would require a `docker-compose.tls.yml` overlay that mounts certs and a parallel integration script. Same trade-off as before: doubles the integration-test maintenance surface; deferred.
- `forward_with_retry` always re-fires the same closure on the same target. There's no "retry on a different replica" logic — if peer N is down, all `max_retries` attempts go to peer N. A future "tried-other-replicas" mode would need a different abstraction (a list of `Fn() -> Fut` closures, one per replica) and changes how the caller iterates. Out of scope; the current behavior is correct, just not maximally clever.
- `gen_certs` writes certs that are world-readable (`-rw-r--r--`). Fine for local dev; production deployments using `gen_certs` should `chmod 600` the key files. Could be added to the binary itself; deferred (production flows should be using a real PKI anyway).
- Documentation surface (`README.md`, `docs/security.md`, `docs/mcp-setup.md`, `clients/ferrocache-skill.md`, `clients/python/README.md`, plus all the `Claude.md` arch decisions) is now wide. Future drift between them (e.g. someone adds a new env var and updates only one) is a maintenance hazard. A scripted check that all env-var names mentioned in any `.md` exist in `config.rs` would catch this; deferred.
- The audit didn't add any test that *verifies* the absence of a leak — it inspected the code by reading. A proper "fuzz error responses and assert no FERROCACHE_AUTH_TOKEN appears in the body" test suite would be more durable. Realistic given the small response surface, but lower priority than shipping the user-visible fixes.
- This is the last planned mission. ferrocache is now: 99 Rust tests + 39 Python tests + 20 cluster integration tests passing; auth + mTLS + retry + observability + WAL compaction + gossip-driven cluster all opt-in or default-safe; documented end-to-end. Future work would be operator-driven (real workloads surfacing real edge cases) rather than feature-driven.
