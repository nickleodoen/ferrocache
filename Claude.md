# ferrocache — Project Context

## Section 1: Evergreen

**What this is:** Distributed semantic cache for LLM applications, written in Rust. Single binary, multi-node via consistent hashing + gossip replication. Portfolio project for big-tech Core SWE interviews.

**Current phase:** Phase 7 complete. v1.0 ready. 30 missions across 7 phases.

**Completed work:**
- M1: axum scaffold (3 routes, tracing, env-based port)
- M2: hnsw_rs integration (DistCosine, side-table keyed by usize, dim lock)
- M3: WAL (NDJSON, fsync-per-insert, replay on startup, corrupt-line skip; WAL-first insert path; UUIDs stable across restarts)
- M4: config crate (TOML + env merge), request validation (4096 dim, 100KB resp, threshold range), /stats endpoint, dead-code cleanup
- M5: consistent hash ring (FNV-1a, 64 vnodes default) + chitchat gossip discovery, /cluster/status endpoint, single-node fallback
- M6: cluster-aware /query routing + /insert synchronous replication via reqwest, `?local=true` loop-prevention, coordinator UUID stamping, get_n_nodes replica walk
- M7: Dockerfile + docker-compose 3-node cluster + bash integration tests; fixed env-var `prefix_separator` config bug (latent since M4)
- M8: README + Mermaid diagram, criterion benchmarks (real numbers), stdlib-only Python client, GitHub Actions CI (check + integration), library/binary split for bench imports
- M9: simulation harness (`tests/simulate.py` + `simulate_no_ml.py`) — FAQ workload with sentence-transformers, 100% hit rate / 0 false positives at 0.90 threshold
- M10: drop-in SDK middleware (`wrap_openai`, `wrap_anthropic`) with proxy attribute delegation, fail-open on cache outage, env-var config; restructured Python client into a package
- M11: framework backends — `FerrocacheCache` (LangChain `BaseCache`) + `FerrocacheLLM` (LlamaIndex `CustomLLM` subclass); optional imports, fail-open default, custom `embed_fn` supported
- M12: MCP server (`ferrocache.mcp_server`) over stdio — 3 tools (`semantic_cache_lookup`, `semantic_cache_store`, `cache_status`), text-in/JSON-out (embedding handled internally), Claude Desktop + Claude Code setup docs
- M13: distribution — `pyproject.toml` with optional extras (`[openai]`, `[anthropic]`, `[langchain]`, `[llamaindex]`, `[mcp]`, `[all]`); base `pip install ferrocache` is zero-deps; root + Python LICENSE; release CI on `v*` tags publishes Docker image to GHCR and Python package to PyPI via OIDC trusted publishing
- M14: namespace partitioning — `SemanticIndex` is a `HashMap<model_id → NamespacedIndex>`, each with its own HNSW + side-table; `model_id` required on `/insert` and `/query`; Python integrations auto-derive `model_id` from default embed model; PyPI bumped to 0.2.0; pre-M14 WAL entries migrate to `legacy::unknown`
- M15: WAL compaction + snapshotting — `src/snapshot.rs` writes `magic+version+wal_seq+bincode(Vec<SnapshotEntry>)` atomically; WAL gained monotonic `sequence` numbers; startup loads snapshot then replays only `seq > watermark`; auto-compaction every 10K inserts (configurable) + manual `POST /admin/compact`; `CacheEntry` re-stores `embedding`+`query_text`
- M16: Prometheus `/metrics` (hand-written text exposition, no `prometheus` crate) — atomic counters with per-namespace `RwLock<HashMap>`, fixed 16-bucket `LatencyHistogram` (100µs → 10s), `/stats` JSON gains `counters` block, monitoring compose overlay (Prometheus + Grafana on :9090/:3100) with provisioned datasource and 8-panel ferrocache dashboard
- M17: bearer token auth — opt-in via `FERROCACHE_AUTH_TOKEN`; `subtle::ConstantTimeEq` compare; `/health` and `/metrics` exempt; inter-node forwards carry the same token; Python client reads env automatically; integrations gain `auth_token=` kwarg
- M18: cluster mTLS — opt-in via `FERROCACHE_CLUSTER__TLS__ENABLED`; rcgen 0.13 + rustls 0.23; second TLS listener on `internal_port` (default `port + 1000`) requires client cert chained to cluster CA; `WebPkiClientVerifier` rejects anonymous clients at handshake; `gen_certs` binary writes dev CA + per-node cert/key; production loads PEMs from `cluster.tls.{ca_cert,node_cert,node_key}_path`
- M19: replication retry (exponential backoff + ±20% jitter, retries on connect/timeout/5xx, never 4xx) + security audit (scrubbed peer IDs / WAL paths / snapshot paths from HTTP error bodies, kept in tracing logs) + `docs/security.md` threat-model/deployment-recipes/known-limitations + `clients/ferrocache-skill.md` LLM reference; `replication_retries_total` metric distinct from `replication_failures_total`
- M20: WAL group-commit — inserts push `WalCommand::Insert` to mpsc channel, single background flush task batches up to `wal_batch_size` (default 256) requests within `wal_batch_timeout_ms` (default 1ms), one fsync per batch; flush task is sole writer (compaction goes through same channel); 7.5× insert throughput at concurrency 100; new `tests/bench_concurrent.py` harness
- M21: phi accrual failure detector — `src/failure_detector.rs` with `PhiAccrualDetector` (sliding window of inter-arrivals, normal CDF via Abramowitz & Stegun erf, `phi = -log10(1 - F(elapsed))`), `PeerStatus::{Alive, Suspected, Dead}` state machine; heartbeats piggyback on chitchat reconciler; `peer_health` map in `/cluster/status`, `ferrocache_peer_phi` gauge in `/metrics`; soft routing skips Dead peers (query → miss instead of 502, insert fan-out → degraded replication)
- M22: ring reassignment + failover — reconciler decoupled from chitchat liveness (only phi-accrual `Dead` triggers ring removal; `Suspected` stays); `dead_nodes: HashSet<String>` on `ClusterState` surfaced via `/cluster/status.dead_nodes`; re-joining nodes added back to ring AND removed from dead set on the same tick; zero data movement (clockwise successor already has data via replication_factor=2); `dead_node_removal_enabled` flag; `ferrocache_ring_changes_total` counter
- M23: read repair — coordinator fan-outs to non-dead replicas on miss, returns first hit, spawns async repair via `/internal/entry/{uuid}` + WAL group-commit; `LocalParam.repair: Option<bool>` three-state (none/forwarded/fan-out); UUID reverse lookup in `SemanticIndex`; `read_repair_enabled` config flag; `read_repairs_total` + `read_repair_failures_total` counters; eventually-consistent healing through traffic, no Merkle trees
- M24: per-entry access tracking — `CacheEntry` gains `inserted_at` (WAL-persisted), `last_accessed_at` + `access_count` (snapshot-persisted, in-memory soft state); query hit takes a brief index write lock to bump them (misses skip); `QueryHit.internal_id` exposed so the handler avoids a second lookup; `/admin/entry-stats` returns top-N most-accessed per namespace; `/stats` namespaces gain `oldest_entry_ts` / `newest_entry_ts` / `total_accesses`; `now_unix_secs()` helper for stamping; `FullEntryResponse` carries access metadata over the wire so read-repair preserves history
- M25: LRU eviction + lazy HNSW deletion — `HnswConfig.max_entries_per_namespace: Option<usize>`, `WalEntry.tombstone: bool` + `WalEntry::tombstone_entry()`; `evicted_ids: HashSet<usize>` per namespace + query oversampling 1+min(ghosts, 8) candidates; `evict_lru` with `(last_accessed_at, inserted_at, internal_id)` deterministic tie-break; `rebuild` triggered at 20% ghost ratio; flush task runs `evict_to_cap_step` after each batch (single-fsync tombstone batch); main.rs replay branches on `entry.tombstone` → `remove_by_uuid`; metrics: `evictions_total`, `index_rebuilds_total`, per-ns `evictions` + `evicted_ghost_count` gauge
- M26: TTL + targeted deletion + semantic invalidation — `InsertRequest.ttl_seconds`, `WalEntry/SnapshotEntry/CacheEntry.expires_at: Option<u64>`; query path checks expiry inline (no state mutation); background `expiry_reaper_loop` (default 60s, env-tunable, 0 disables) sweeps `collect_expired` and tombstones via WAL channel; startup expiry pass before listener binds; `DELETE /entry/:uuid` (cluster fan-out to ALL non-dead peers, 404 = idempotent); `POST /admin/invalidate` (O(n) cosine sim sweep + tombstones + per-replica fan-out); standalone `cosine_similarity()`; metrics: `expirations_total`, `deletions_total`, `invalidations_total` (top-level + per-namespace); Python client gains `delete_entry`, `invalidate`, `insert(..., ttl_seconds=)`
- M27: exact-match pre-filter — per-namespace `exact_match_index: HashMap<normalized_text, uuid>` checked BEFORE HNSW when `query_text` is on `/query`; standalone `normalize_query_text` (lowercase + trim + whitespace-collapse, no Unicode); `query_with_text(...)` is the new entry point and `query(...)` becomes a backward-compat wrapper; `QueryHit.exact_match: bool` + `QueryResponse.exact_match`; private `drop_entry(internal_id)` consolidates ALL removal paths so exact-match cleanup never gets forgotten; UUID ownership check on cleanup handles delete-then-reinsert correctness; `ferrocache_exact_match_hits_total` + per-ns counter (additive to `queries_hit`); Python middleware/LangChain/LlamaIndex/MCP all forward `query_text` automatically — no explicit caller change needed
- M28: cache scope + tenant isolation — `cache_scope: Option<String>` on `InsertRequest`/`QueryRequest`/`InvalidateRequest`; standalone `effective_namespace(model_id, cache_scope) -> "{model_id}::{scope}"` (or just `model_id` when absent / whitespace-only); WAL `model_id` field IS the effective namespace (no schema change, no migration); insert/query/invalidate handlers compute `ns` once and pass it through index, metrics, and tombstones; `max_entries_per_namespace` applies per scoped namespace (resource isolation comes free); Python client + middleware + LangChain + LlamaIndex + MCP all gain `cache_scope` kwarg
- M29: multi-turn conversation scoping — `conversation_id: Option<String>` on insert/query; `conversation_namespace(model_id, scope, conv_id) -> "{base}::conv_{id}"` (hardcoded `conv_` prefix prevents collision with `cache_scope`); inserts go to conversation namespace ONLY; queries do two-level lookup (conversation → base fallback) and `QueryResponse.scope` reports `"conversation"`/`"global"` accordingly; `conversation_ttl_seconds` config auto-stamps TTL on conv inserts (explicit `ttl_seconds` overrides); reaper sequence is `collect_expired → rebuild_dirty_namespaces → prune_empty_namespaces` so empty `::conv_` namespaces are reclaimed; latent bug fixed: `replay_entry` now branches on `WalEntry.tombstone` so reaper-driven tombstones routed through the WAL channel correctly delete (was re-materialising phantom empty entries since M26)
- M30: Phase 7 integration + v1.0 docs — full suite green (222 Rust + 59 Python + 44 integration assertions including 12 new Phase 7 cluster tests); `simulate.py --ttl` flag exercises inline expiry; `tests/benchmark_vs_gptcache.py` runs same workload against ferrocache + GPTCache (subprocess-isolates GPTCache so faiss/onnx native crashes don't propagate); README rewritten for the complete v1.0 surface (eviction & expiry, tenant isolation, conversation scoping, GPTCache comparison table); `clients/ferrocache-skill.md` rewritten as the v1.0 LLM-agent reference (full API, config, prod checklist, troubleshooting); Makefile gains `benchmark-vs-gptcache` target; no behavioural code changes

**Module map:**
- lib.rs — re-exports modules so benches and external consumers can import
- main.rs — entry, tracing init, config load, WAL replay, optional cluster init, serve
- server.rs — router, handlers, request validation, routing/replication coordination, tests
- index.rs — `SemanticIndex` (hnsw_rs wrapper + HashMap side-table), `replay_entry`/`replay_snapshot_entry`; per-namespace `uuid_to_internal: HashMap<String, usize>` plus top-level `uuid_to_namespace: HashMap<String, String>` so `get_entry_by_uuid -> Option<FullEntry>` is O(1) (used by /internal/entry/{uuid} for read repair); insert path de-dupes on UUID so re-inserting the same entry is idempotent; `CacheEntry` carries `inserted_at`/`last_accessed_at`/`access_count` (M24); `record_access(model_id, internal_id)` bumps the latter two under the index write lock; `top_entries_per_namespace(N)` powers `/admin/entry-stats`; `now_unix_secs()` helper exported for stamping `inserted_at` at the insert site; M25 adds `evicted_ids: HashSet<usize>` per namespace (HNSW lazy deletion), `evict_lru` / `remove_by_uuid` / `needs_rebuild` / `rebuild`, top-level `evict_to_cap(max)` + `rebuild_dirty_namespaces`; query oversamples by `1 + evicted_ids.len().min(8)` and filters ghost ids before threshold check; M26 adds `expires_at: Option<u64>` on `CacheEntry`/`WalEntry`/`SnapshotEntry`/`FullEntry`, query path also filters expired neighbors (without mutation), `collect_expired(now)` (top-level + per-ns) for the reaper, `find_similar(embedding, threshold, model_id, now)` + `invalidate_similar` for `/admin/invalidate`, `remove_internal(id)` + `namespace_for(uuid)` helpers, standalone `cosine_similarity` for the explicit similarity sweep; M27 adds per-namespace `exact_match_index: HashMap<normalized_text, uuid>` checked before HNSW; `query_with_text(...)` is the M27 entry point and `query(...)` is now a backward-compat wrapper that calls it with `None` text; standalone `normalize_query_text()` (lowercase + trim + whitespace-collapse); private `drop_entry(internal_id)` helper now centralises every removal path so exact-match cleanup is uniform; `QueryHit.exact_match: bool` distinguishes pre-filter hits from ANN; M28 adds standalone `effective_namespace(model_id, cache_scope) -> String` that returns `"{model_id}::{scope}"` when scope is non-empty after trim, else `model_id` — server handlers use it to compose the namespace key, the WAL `model_id` field IS that effective namespace (no separate `cache_scope` on disk); M29 adds `base_namespace` (alias for `effective_namespace`) + `conversation_namespace(model_id, cache_scope, conversation_id) -> "{base}::conv_{id}"` (hardcoded `conv_` prefix prevents collision with cache_scope), and `prune_empty_namespaces()` on `SemanticIndex` drops conv-prefixed namespaces whose `entries` and `evicted_ids` are both empty (called by the reaper after `rebuild_dirty_namespaces`); `replay_entry` now branches on `entry.tombstone` so reaper-driven tombstones routed through the WAL channel correctly delete instead of re-materialising phantom entries
- wal.rs — `Wal` (append, fsync, sequence, truncate, replay) + `GroupCommitWal` (mpsc-fed flush task that batches writes and fsyncs once per batch); `WalCommand::{Insert,Compact}`, `InsertError::{Wal,Index}`, `GroupCommitConfig`; flush task owns the only `Wal` handle and the index write lock
- models.rs — request/response DTOs (InsertRequest carries optional `uuid`)
- config.rs — FerrocacheConfig (port, wal_path, hnsw, cluster, compact_interval_inserts, auth_token, wal_batch_size, wal_batch_timeout_ms), HnswConfig, ClusterConfig (gossip_addr, api_addr, replication_factor, seed_nodes, tls, max_replication_retries)
- state.rs — AppState { node_id, index, wal: GroupCommitWal, wal_path, snapshot_path, hnsw_config, cluster, router, replication_factor, metrics, auth_token, max_replication_retries }
- auth.rs — `auth_middleware` (axum `from_fn_with_state`); constant-time `subtle::ConstantTimeEq`; `/health` + `/metrics` exempt; only installed when `auth_token` is non-empty
- tls.rs — `TlsBundle`, `generate_ca`/`generate_node_cert` (rcgen 0.13), `load_or_generate` (disk vs in-memory dev), `build_server_config` (rustls 0.23 mTLS — `WebPkiClientVerifier` requires a client cert chained to the cluster CA), `install_default_crypto_provider` (idempotent aws-lc-rs install); used by `main.rs` for the internal listener and `router.rs` for the reqwest identity
- bin/gen_certs.rs — dev-cert generator binary (`cargo run --bin gen_certs [node_ids...]`); writes `./certs/ca.pem` + `./certs/<node_id>/{cert,key}.pem`; intentionally not built into the production Docker image
- ring.rs — HashRing (BTreeMap<u64, node_id>, FNV-1a, virtual nodes, get_n_nodes for replica walk)
- cluster.rs — ClusterState wrapping chitchat; `reconcile_step` (extracted free fn so tests drive it without UDP) feeds the phi accrual detector then drives ring membership: ring ADDs come from chitchat snapshot vs ring-set diff (with re-join detection vs `dead_nodes`), ring REMOVALS come from detector `Dead` status (M22), never from chitchat `live_nodes` directly; `dead_nodes: HashSet<String>` tracks ring-removed peers for `/cluster/status`
- failure_detector.rs — `PhiAccrualDetector` (sliding window of inter-arrival times per peer, normal CDF via Abramowitz & Stegun erf, phi=-log10(1-F(elapsed))); `PeerStatus::{Alive, Suspected, Dead}`; injectable `_at(Instant)` methods so tests don't depend on wall-clock; passive — never sends heartbeats, only observes
- router.rs — ClusterRouter (reqwest client) — forward_query / forward_insert return typed `reqwest::Result` so the `forward_with_retry` helper can classify failures (retry on connect errors / timeouts / 5xx; never on 4xx); ±20% jitter from `rand::thread_rng()`; metric `ferrocache_replication_retries_total` increments inside the helper
- snapshot.rs — `SnapshotEntry` + `write_snapshot`/`read_snapshot` (magic+version+wal_seq+bincode); `compact()` helper that writes the snapshot and truncates the WAL atomically
- metrics.rs — `Metrics` (atomic counters + per-namespace `RwLock<HashMap<NamespaceMetrics>>`), `LatencyHistogram` (16 fixed buckets 100µs–10s), `render()` to Prometheus text exposition format
- benches/cache_bench.rs — criterion: insert / query_hit / query_miss / insert_with_wal / snapshot_write_10k
- tests/bench_concurrent.py — concurrent throughput harness (HTTP via FerrocacheClient at concurrency 1/10/50/100; insert + query_hit + query_miss); `make bench-concurrent`
- clients/python/ferrocache.py — stdlib-only Python client (urllib + json)

**Architecture decisions (append-only, one line each):**
- Client computes embeddings externally; ferrocache stores/compares f32 vectors
- Crates: tokio, axum 0.7, hnsw_rs, chitchat 0.10, tracing, serde_json, config, reqwest
- WAL format: newline-delimited JSON, replayed on startup, fsync per insert
- Consistent hashing key: u64 from first 8 bytes of embedding (first 2 f32 values, big-endian)
- Cosine distance: hnsw_rs DistCosine returns 1-similarity; we convert
- Side-table: HashMap<usize, CacheEntry> keyed by hnsw internal id; UUID inside CacheEntry
- WAL is source of truth; production insert path is WAL-first then replay_entry
- insert() (UUID-generating) is #[cfg(test)] only; production uses replay_entry
- Config priority: env vars (FERROCACHE_ prefix, `_` prefix-sep, `__` section-sep, `,` list-sep) > ferrocache.toml > defaults
- tokio::sync::RwLock for index; WAL is owned by a single background flush task (no shared mutex)
- Validation limits (server.rs constants): MAX_EMBEDDING_DIM=4096, MAX_RESPONSE_BYTES=102_400
- Threshold accepted range on /query: 0.0..=1.0
- Hash ring: FNV-1a (cross-process deterministic), 64 virtual nodes per physical node by default
- Cluster mode is opt-in via `cluster.enabled`; `false` keeps Phase 1 behavior bit-identical
- Ring reconciler runs every 2s in a background task; chitchat gossip interval = 1s
- Each node advertises its API addr via chitchat KV under key `api_addr`; reconciler reads peers' addrs
- Routing: `?local=true` skips ring lookup (used by forwarded requests + debugging); coordinator stamps UUID before fan-out
- Replication: synchronous, primary-or-coordinator + (replication_factor-1) replicas; any replica failure → 502
- Forward failures return 502 (BAD_GATEWAY) to distinguish "peer failed" from local 500
- Docker runtime image needs `libgomp1` (hnsw_rs → rayon → OpenMP)
- `seed_nodes` is parsed from a comma-separated env string via config-rs `list_separator(",")`+`with_list_parse_key`
- `Wal::open` mkdir's parent dir so containers can mount an empty `/data` volume
- Crate is library + binary (lib.rs re-exports modules); `cargo build --release --bin ferrocache` for prod images
- Benchmarks via criterion; Python client uses only stdlib (urllib+json)
- Python package on PyPI with optional extras; Docker image on GHCR; release CI on version tags
- Index is namespace-partitioned by `model_id`; each namespace has its own HNSW instance + side-table
- `model_id` is required on `/insert` and `/query`; old WAL entries without it default to `legacy::unknown`
- `model_id` format convention: `model_name::dimension` (e.g. `all-MiniLM-L6-v2::384`)
- Cross-namespace queries are impossible by construction — vectors from different models never compare
- Snapshot format: `magic (FERROSNA) + version (u64=1) + wal_sequence (u64) + entry_count (u64) + bincode-encoded Vec<SnapshotEntry>`; written atomically via temp+rename so a crash never corrupts the prior snapshot
- Compaction: snapshot the side-table entries, then truncate the WAL — HNSW is rebuilt from stored embeddings on load (no need to serialize the graph itself)
- WAL entries carry a monotonic `sequence: u64` that never resets — startup skips entries `<= snapshot_watermark` so the WAL tail replay is bounded
- Auto-compaction every N inserts (default 10K via `compact_interval_inserts`, 0 disables); manual via `POST /admin/compact`
- `CacheEntry` again stores `embedding` + `query_text` so snapshots can dump the full record (re-added in M15; was dropped in M4)
- Prometheus `/metrics` is hand-written (no `prometheus`/`metrics` crate); atomic counters with `Ordering::Relaxed` + a fixed-bucket `LatencyHistogram`
- Per-namespace hit/miss/insert counters carry a `namespace` label; global query/insert latency histograms have 16 buckets (100µs → 10s) plus a `+Inf` bucket
- `/stats` JSON gained a `counters` object so users without Prometheus can still read the same numbers
- Monitoring stack lives in `monitoring/` as a docker-compose overlay (`-f docker-compose.yml -f monitoring/docker-compose.monitoring.yml`); base cluster runs unchanged without it
- `CacheEntry` again stores `embedding` + `query_text` so snapshots can dump the full record (re-added in M15; was dropped in M4)
- Bearer token auth via `FERROCACHE_AUTH_TOKEN` env var; disabled when unset (opt-in, backward compatible)
- Auth exempts `/health` and `/metrics` (load balancers and Prometheus need unauthenticated access)
- Token comparison uses `subtle::ConstantTimeEq` to prevent timing attacks; the token value is never logged at any level
- Inter-node replication forwards include the same bearer token (shared-secret cluster model — all nodes carry the same `FERROCACHE_AUTH_TOKEN`)
- mTLS between cluster nodes via rustls 0.23 + rcgen 0.13; opt-in via `cluster.tls.enabled`; second TLS listener on `internal_port` (default `port + 1000`) serves the same router; public port stays plain HTTP
- Dev mode (no cert paths) auto-generates a self-signed CA + leaf in memory; production loads PEMs from `cluster.tls.{ca_cert,node_cert,node_key}_path`
- TLS reqwest client trusts ONLY the cluster CA (system roots disabled) and presents the node leaf as client identity — i.e. mTLS, not just TLS
- Gossip (chitchat UDP) remains unencrypted: it carries only ring metadata (node IDs + forwarding addresses), not cached data
- `cargo run --bin gen_certs [node_ids...]` writes `./certs/ca.pem` + `./certs/<node>/{cert,key}.pem` for local clusters; the production Dockerfile builds only `--bin ferrocache`, so `gen_certs` never ships in images
- Replication retry: exponential backoff (50ms × 2^attempt, ±20% jitter, capped at 5s), max 3 attempts by default; retries on connect errors + timeouts + 5xx only, never on 4xx (deterministic failures)
- `ferrocache_replication_retries_total` metric tracks retry pressure separately from final failures — retries that succeed do NOT count as failures, so dashboards can distinguish "noisy network, eventually succeeded" from "peer permanently down"
- WAL group-commit (M20): inserts push `WalCommand::Insert` to an mpsc channel; a single background flush task owns the `Wal`, batches up to `wal_batch_size` requests (default 256) or `wal_batch_timeout_ms` (default 1ms) elapses, writes the entire batch in one `write(2)` and one `fsync(2)`, then takes the index write lock once and inserts every entry; per-request oneshot replies fire after the lock is released. `wal_batch_size = 1` degrades to per-insert fsync (pre-M20 behavior) and is what tests use.
- Insert throughput scales with concurrency under group-commit (measured: 7.5× at concurrency 100 vs per-insert fsync); single-client insert is slightly slower (batch timeout adds ~1ms wait); query throughput is unaffected (read path doesn't fsync)
- The flush task is the SOLE writer to the WAL — `compact` is also a `WalCommand` so it serializes with insert flushes (FIFO via the same channel); auto-compaction's `inserts_since_compact` counter lives in the flush task only (no atomics, no locking, single producer)
- Insert handler error surface: `InsertError::Wal` → 500 "persistence failed" (paths/error chain stay in logs), `InsertError::Index` → 500 with the underlying message (e.g. "dimension mismatch") preserving M19 audit shape
- Phi accrual failure detector per peer: sliding window of heartbeat inter-arrivals (default 100 samples), normal CDF, phi = -log10(1-F(elapsed)); threshold 8.0 default (Cassandra/Akka standard); std-dev floored at 100ms so a perfectly regular heartbeat stream still has a defined CDF
- Heartbeats piggybacked on chitchat gossip — the existing 2s reconciler tick records a heartbeat for each peer chitchat reports as live, then runs `check_all`; no separate heartbeat task
- Three peer states: `Alive → Suspected (phi ≥ threshold) → Dead (phi ≥ 2×threshold)`; recovery to `Alive` on any new heartbeat (Dead is not permanent — ring removal is M22's job)
- Dead peers skipped in query routing (immediate miss instead of retry-then-502) and insert fan-out (degraded replication with warn log naming `dead_peers` and `effective_replicas`); soft optimization, retry logic still runs if a "dead" peer races back into a request
- Per-peer phi exposed as `ferrocache_peer_phi{peer=...}` gauge plus `ferrocache_peers_suspected` / `ferrocache_peers_dead` gauges in /metrics; `peer_health` map in /cluster/status JSON
- Ring membership decoupled from chitchat liveness (M22): ring removals driven exclusively by phi accrual `Dead` status; `Suspected` peers stay in the ring (might recover); `chitchat.live_nodes()` controls discovery + KV propagation only
- Dead nodes tracked in `dead_nodes: HashSet<String>` on `ClusterState`, surfaced via `/cluster/status.dead_nodes`; re-joining nodes (reappear in chitchat snapshot) are added back to ring AND removed from the dead set on the same reconcile tick
- Ring reassignment is automatic and zero-data-movement: removing a dead node's virtual nodes from the `BTreeMap`-backed `HashRing` makes its arc fold into the clockwise successor, which is already a replica thanks to `replication_factor=2` — the cache property (no durable single-source-of-truth contract) means we don't need anti-entropy to ship data; missed entries are recomputed by the LLM on the next miss
- `failure_detector.remove_peer` is NOT called from the reconciler — the detector keeps history across chitchat-driven drops so phi can keep climbing into Dead even after gossip stops reporting the peer; only when an operator actually removes a node from the cluster would `remove_peer` apply (out of scope)
- Re-joined nodes serve stale data until the next anti-entropy pass (M23 will add this); stale misses are acceptable in cache semantics — the client re-calls the LLM, the response is cached on the new owner, ring is consistent again
- `dead_node_removal_enabled` config flag (default `true`) supports a monitoring-only mode where phi reports without ring mutation — useful for canary rollouts of the failover behaviour
- Ring change observability: `ferrocache_ring_changes_total` counter (every add or remove from the reconciler) + `ferrocache_ring_members` gauge mirror what's in the ring; bumped from `Metrics::record_ring_change()` inside `reconcile_step`
- Read repair on query miss (M23): coordinator (= ring owner that received the forwarded query) queries non-dead replicas in parallel via `futures::future::join_all`, returns first hit to client immediately, spawns `tokio::spawn` background task that fetches full entry via `/internal/entry/{uuid}` then re-inserts locally through the WAL group-commit channel (full durability); failures (network, 404, channel closed) increment `read_repair_failures_total` but never crash
- `/internal/entry/{uuid}` returns `FullEntryResponse { uuid, embedding, response, query_text, model_id }` — embedding is required so the coordinator can re-insert; 404 maps to `Ok(None)` in `forward_get_entry`; `/internal/read-repair` is a UUID-mandatory `/insert` variant going through the same WAL channel
- `LocalParam.repair: Option<bool>` semantics: `None` defaults to "repair if NOT explicitly local-only" (so client `?local=true` skips repair, but the coordinator's forwarded `?local=true&repair=true` triggers it); `Some(false)` set by the read-repair fan-out itself prevents recursion; `Some(true)` set by the coordinator's `forward_query` makes the recipient know it's the canonical primary
- UUID reverse lookup populated on every `replay_entry`/`replay_snapshot_entry` (not just new inserts) — old WAL/snapshot replay paths also benefit from O(1) uuid lookup
- Read repair is eventually consistent — re-joined nodes heal as traffic naturally queries their stale arcs; entries never queried again are never repaired (acceptable for a cache, since a forgotten entry just means the LLM gets called again on its next visit)
- No Merkle trees, no background reconciliation, no bulk transfer — read repair only, one entry at a time. Phase 6 spec deliberately picked this over the more complex Cassandra-style anti-entropy because the cache semantics make eventual healing through traffic perfectly correct.
- `read_repair_enabled` config flag (default `true`) gates the entire fan-out — when false, a coordinator miss returns immediately without touching replicas; phi monitoring still runs
- `CacheEntry` tracks `inserted_at` (set once, persisted in WAL), `last_accessed_at` (updated in-memory on every hit, persisted in snapshots), `access_count` (same)
- Access metadata is soft state: WAL stores only `inserted_at`; `last_accessed_at` and `access_count` are snapshot-persisted but lost on crash (rebuild naturally via traffic) — the alternative (a WAL write per query hit) would defeat the cache
- Query hits take a brief index write lock for metadata update (HashMap lookup + 2 field writes, sub-µs); misses stay read-only
- `/admin/entry-stats` exposes top-10 most-accessed entries per namespace for ops visibility — O(n log n) sort per namespace, fine for an admin endpoint
- `QueryHit` carries `internal_id: usize` so the handler can call `record_access` without a second HNSW or HashMap lookup
- Pre-M24 WAL entries lack `inserted_at` and deserialize to 0; pre-M24 snapshots default the three new fields to 0 via `#[serde(default)]` — entries with `inserted_at == 0` are treated as "oldest" by future LRU (M25), the correct behaviour for entries from before access tracking
- LRU eviction via `max_entries_per_namespace` (default unlimited): on overflow, evict the entry with smallest `last_accessed_at`; O(n) scan per eviction (acceptable at cache scale)
- LRU tie-break order: `(last_accessed_at, inserted_at, internal_id)` — when timestamps tie (sub-second insert burst), the smaller internal id evicts first, giving deterministic FIFO-on-tie behaviour matching "oldest entry first"
- HNSW lazy deletion: evicted internal IDs tracked in `evicted_ids: HashSet<usize>`; query results filter against it before threshold check; search requests `1 + evicted.len().min(8)` extra candidates to compensate
- HNSW rebuild triggered when `evicted_ids.len() / (entries.len() + evicted_ids.len()) > 0.20`; rebuilds from scratch using only live entries; runs inline in the flush task under the write lock
- WAL tombstones: `{"tombstone":true,"uuid":"...","model_id":"...","sequence":N}` — on replay, remove the entry instead of inserting; `embedding`/`response`/`query_text` empty/default; prevents evicted entries from returning after restart
- Snapshots exclude evicted entries by construction (side-table only has live data); tombstones in the WAL tail override snapshot entries on replay
- Eviction runs inline in the flush task after each insert batch; the index write lock is acquired once, eviction + rebuild + tombstone collection happen under it, then the lock drops and tombstones are written as a single fsync — eviction is a soft cap (may briefly exceed by `batch_size` entries)
- TTL via `ttl_seconds` on insert → `expires_at = inserted_at + ttl` stored in WAL/snapshot; expired entries return miss on query (checked inline, no state mutation); background reaper cleans up + writes tombstones
- Background expiry reaper runs every `expire_scan_interval_secs` (default 60s; 0 disables); scans all namespaces under write lock, evicts expired entries, writes tombstones through the WAL channel one by one (flush task batches them); startup does one `collect_expired` pass before binding the listener
- `DELETE /entry/:uuid` removes from index + writes tombstone + fans out to all live peers (NOT ring-based — UUID has no embedding to hash); 404 from a peer is idempotent success
- `POST /admin/invalidate` finds entries with cosine sim ≥ threshold via O(n) side-table scan + evicts + tombstones + fans out to replicas (each replica computes its own matches with the same `(embedding, threshold)` — no UUID list shipped, contract is "compute, not copy")
- Three invalidation metrics with the same per-namespace + global pattern as M25 evictions: `expirations_total` (TTL), `deletions_total` (explicit DELETE), `invalidations_total` (semantic radius)
- The reaper task is a separate `tokio::spawn`, NOT part of the flush task — separation of concerns; the flush task stays the sole WAL writer, the reaper sends tombstones through the WAL channel like any other producer
- Exact-match pre-filter (M27): `HashMap<normalized_query_text, uuid>` per namespace, checked BEFORE HNSW ANN search when `query_text` is present on `/query`; O(1) vs O(log n) for HNSW. Hit reports `similarity: 1.0` and `exact_match: true`; ANN hits report `exact_match: false`; misses omit the field entirely
- Text normalization: `text.split_whitespace().map(str::to_lowercase).join(" ")` — lowercase + trim + whitespace-collapse. No stemming, no Unicode normalization (NFKD etc.) so behaviour stays predictable
- Single removal helper `NamespacedIndex::drop_entry(internal_id)` is the SOLE point that mutates the side-table on removal — every public path (`evict_lru`, `remove_by_uuid`, `remove_internal`, `collect_expired_within`) funnels through it, so exact-match cleanup never gets forgotten
- Exact-match cleanup uses a UUID ownership check: only remove the key when its current value still references the dropped UUID — protects against a delete-then-reinsert losing the new entry's mapping
- HNSW rebuild does NOT touch `exact_match_index`: UUIDs are stable across rebuild, so the existing mappings remain correct
- Python middleware + LangChain + LlamaIndex + MCP all forward `query_text` to `client.query()` automatically — no explicit caller change needed for the exact-match pre-filter to fire
- `ferrocache_exact_match_hits_total` is ADDITIVE to `ferrocache_queries_hit` — every exact-match is also a regular hit, the new counter is a subcategory not an alternative
- `cache_scope: Option<String>` on insert/query → effective namespace = `"{model_id}::{scope}"` when present, `model_id` when absent; the WAL stores the effective namespace as `model_id` — no new WAL field, backward-compatible (M28)
- Scope is entirely user-defined — ferrocache doesn't interpret the content; common uses: tenant ID, user ID, temperature, system prompt version, or any combination thereof
- Empty/whitespace-only scope treated as absent via `effective_namespace` utility — defensive against `cache_scope: ""`
- Scoped namespaces are first-class — each gets its own HNSW, side-table, eviction cap, metrics, exact-match index; cross-scope queries are impossible by construction
- Python client + middleware + LangChain + LlamaIndex + MCP all gain `cache_scope` kwarg, forwarded through to insert/query/invalidate
- `conversation_id: Option<String>` on insert/query → conversation namespace = `"{base}::conv_{id}"` where base is `effective_namespace(model_id, cache_scope)`; inserts go to conversation namespace only; queries do two-level lookup: conversation first, then base fallback (M29)
- `QueryResponse.scope: Option<String>` — `"conversation"` or `"global"` when `conversation_id` is present; `None` when absent (backward-compatible)
- `conversation_ttl_seconds` config (default None) auto-sets TTL on conversation-scoped inserts; explicit `ttl_seconds` on the request overrides; M26 reaper handles actual expiry
- Empty conversation namespaces (all entries expired/evicted) are pruned by the reaper via `prune_empty_namespaces` — only namespaces containing `::conv_` are pruned; base namespaces survive empty; the reaper sequence is `collect_expired → rebuild_dirty_namespaces → prune_empty_namespaces` so the rebuild clears `evicted_ids` before the prune predicate runs
- `conv_` prefix prevents namespace collisions between conversation IDs and cache scopes; reserved as a forbidden prefix for `cache_scope` (documented, not enforced)
- Bug fix shipped with M29: `replay_entry` now branches on `WalEntry.tombstone` and calls `remove_by_uuid` instead of `insert_with_uuid` — the runtime path that routes reaper-driven tombstones through the WAL channel was previously re-materialising phantom empty entries (latent since M26; main.rs startup replay had its own correct branch but the flush task did not)
- Phase 7 integration validated (M30): 222 Rust unit tests, 59 Python tests, 44 cluster integration assertions (32 baseline + 12 Phase 7 — TTL, scope, conversation, delete, invalidate, exact-match, /admin/entry-stats); GPTCache benchmark harness ships with subprocess isolation so native segfaults (faiss-cpu / onnxruntime on Python ≥ 3.13) don't take down the parent
- `clients/ferrocache-skill.md` rewritten for the complete v1.0 API surface (M30) — supersedes the M19 Phase-5 version; documents every endpoint, every request/response field, full configuration env-var table, production checklist, and the troubleshooting runbook

**Non-negotiable constraints:**
- Auth (bearer token) and cluster mTLS are both opt-in; public-port TLS handled by reverse proxy
- No UI
- No direct OpenAI/Anthropic API calls inside ferrocache
- Use tokio, not async-std
- Every new module gets unit tests before moving on

**Session log rules:**
- Keep only the last 2 session logs below
- When adding a new log, delete the oldest if there are already 2
- Summarize deleted sessions as one-line entries under "Completed work" above

## Section 2: Rolling Session Log (last 2 sessions only)


### 2026-05-04 — Mission 29: Multi-Turn Conversation Scoping
**Built:** Conversation-aware caching with a two-level lookup fallback. Composes with M28 — conversation namespaces are just a third namespace segment under the existing `effective_namespace` scheme. (1) **`InsertRequest.conversation_id: Option<String>`** + **`QueryRequest.conversation_id: Option<String>`** with the `#[serde(default, skip_serializing_if = "Option::is_none")]` shape that's now standard for backward-compatible additive fields. (2) **`QueryResponse.scope: Option<String>`** — `Some("conversation")` when a query with `conversation_id` hits the conversation namespace, `Some("global")` when it falls back to the base namespace, `None` when there was no `conversation_id` (or on miss); skip_serializing keeps the JSON tidy for non-conversation callers. (3) **`base_namespace(model_id, cache_scope)`** + **`conversation_namespace(model_id, cache_scope, conversation_id) -> "{base}::conv_{id}"`** in `src/index.rs` — `base_namespace` is an alias for `effective_namespace` so call sites distinguish "primary level" from "fallback level"; `conversation_namespace` adds the hardcoded `conv_` prefix that prevents collision with cache scopes (`cache_scope="abc"` → `"m::abc"`, `conversation_id="abc"` → `"m::conv_abc"`). (4) **Insert path** — `local_insert_inner` now picks `let ns = match conv_id { Some(cid) => conversation_namespace(...), None => effective_namespace(...) }` and stamps that as `WalEntry.model_id`. Conversation-scoped inserts go ONLY to the conversation namespace by application-level decision; ferrocache doesn't dual-write. (5) **Query path** — `process_query_locally` does an explicit two-level lookup when `conv_id` is present. Level 1: query the conversation namespace. On hit, return with `scope: Some("conversation")`. On miss, level 2 queries the base namespace (uses an `Ok(None) if conv_ns.is_some()` arm before falling through). On hit at level 2, return with `scope: Some("global")`. On both miss, the query records a miss against the BASE namespace (it's the canonical owner from the caller's perspective) and proceeds to read repair as before. Empty/whitespace `conversation_id` is stripped via `as_deref().filter(|s| !s.trim().is_empty())` so the two-level path is bypassed entirely — same defensive pattern as M28. (6) **Auto-TTL for conversation entries** — `FerrocacheConfig.conversation_ttl_seconds: Option<u64>` (env var `FERROCACHE_CONVERSATION_TTL_SECONDS`); threaded into `AppState`; the insert handler computes `effective_ttl = match (conv_id, ttl_seconds) { (_, Some(t)) => Some(t), (Some(_), None) => state.conversation_ttl_seconds, (None, None) => None }`. Explicit `ttl_seconds` always wins; the auto-TTL only fires for conversation-scoped inserts when no explicit value was given. The M26 reaper handles the actual expiry — no new code path. (7) **`SemanticIndex::prune_empty_namespaces() -> Vec<String>`** — drops conversation-prefixed namespaces (key contains `::conv_`) whose `entries` AND `evicted_ids` are both empty. Base namespaces are never pruned (they refill quickly; pruning would just churn allocations). Returns the keys removed for the reaper's log line. (8) **Reaper sequence updated** — `expiry_reaper_loop` now does `collect_expired → rebuild_dirty_namespaces(&hnsw_config) → prune_empty_namespaces` under one write lock. The rebuild step is critical because `collect_expired` adds the freed internal_ids to `evicted_ids`, so without a rebuild the prune predicate (`evicted_ids.is_empty()`) would never fire in practice. To support this, the reaper signature gained `hnsw_config: HnswConfig` and main.rs spawns it with `config.hnsw.clone()`. (9) **Latent bug fix** — `SemanticIndex::replay_entry` now branches on `entry.tombstone` and calls `remove_by_uuid` instead of `insert_with_uuid`. Without this, the runtime path that routes reaper-driven tombstones through the WAL channel (reaper → WAL → flush task → `replay_entry`) was re-materialising a phantom empty entry under the just-deleted UUID. The bug was latent since M26 because (a) main.rs startup replay had its own correct `entry.tombstone` branch, (b) M26 tests asserted via `/metrics` not via re-querying the deleted entry, and (c) M27/M28 didn't touch the reaper. M29 surfaced it because conv-namespace pruning checks `entries.is_empty()` and the phantom re-insert kept that false. The fix lives in `replay_entry` so both startup AND runtime tombstones flow through one branch — main.rs's manual branch is now redundant but retained for explicitness. (10) **Python client** — `insert(..., conversation_id=None)` and `query(..., conversation_id=None)`; sent only when explicitly set so older servers ignore. (11) **Python middleware** — `_intercept` gained `conversation_id` kwarg forwarded to both `cache.query` and `cache.insert`; `_WrappedOpenAICompletions` / `_WrappedAnthropicMessages` / their wrapper classes / `wrap_openai` / `wrap_anthropic` all gained the kwarg with the same store-on-wrapper pattern as M28's `cache_scope`. (12) **LangChain `FerrocacheCache`** + **LlamaIndex `FerrocacheLLM`** — both gained `conversation_id=` to `__init__`, stored as `self._conversation_id`, forwarded to every `client.query` / `client.insert`. (13) **MCP** — `FerrocacheTools.lookup` / `.store` accept `conversation_id`; `_dispatch_tool` forwards `arguments.get("conversation_id")`; `TOOL_DEFINITIONS` exposes the field with a description so an LLM caller knows what it does. (14) **Test fixtures** — `router.rs`, `auth.rs`, and `server.rs` test fixtures got `conversation_id: None` on the InsertRequest/QueryRequest literals (regex sed over the codebase) and an extra `None` arg on every `AppState::new` call site for `conversation_ttl_seconds`. (15) **Tests** — 7 new in `index.rs::tests` (`test_conversation_namespace_function`, `test_conversation_insert_separate_namespace`, `test_conversation_priority_over_global`, `test_no_conversation_id_no_fallback`, `test_prune_empty_conversation_namespace`, `test_prune_does_not_remove_base_namespace`, `test_prune_preserves_conv_namespace_with_evicted_ghosts`); 6 new in `server.rs::tests` (`test_query_with_conversation_id_scope_field`, `test_query_with_conversation_id_conversation_scope`, `test_query_without_conversation_id_no_scope`, `test_conversation_ttl_auto_applied`, `test_explicit_ttl_overrides_conversation_ttl`, `test_conversation_insert_without_conversation_ttl_config`) plus a new `build_state_with_conversation_ttl` helper; 5 new Python tests in a new `tests/test_conversation.py` covering client field pass-through and middleware forwarding. The `test_default_config` in `config.rs` also gained an assertion for `conversation_ttl_seconds.is_none()`.

**Verified:**
- `cargo test --lib` — 222/222 pass (was 209; +13). All deterministic.
- `cargo clippy --all-targets -- -D warnings` — clean (one fix during dev: missing `HnswConfig` import in `main.rs` after threading `hnsw_config` into the reaper).
- `cargo fmt --check` — clean (one auto-fmt pass on the reaper spawn site after adding the new arg).
- `python3 -m unittest discover tests` — 59/59 pass (was 54; +5 in `tests/test_conversation.py`).
- `docker compose up -d --build && ./tests/cluster_integration.sh && docker compose down -v` — 32/32 pass (M21–M28 paths still green; M29 is observable via `/stats` namespace keys + `scope` in query response, no new cluster-level integration assertions added).
- **Conversation smoke test** with the release binary, `FERROCACHE_CONVERSATION_TTL_SECONDS=30 FERROCACHE_EXPIRE_SCAN_INTERVAL_SECS=3`. Inserted a global entry (no `conversation_id`) and a conversation entry (`conversation_id: "conv_1"`). Then four queries: (a) conv-context query for the global question → hit with `scope: "global"` (fallback); (b) conv-context query for the conv question → hit with `scope: "conversation"`; (c) no-conv query for the conv question → miss (no fallback into conv ns); (d) `/stats` showed `test::4` and `test::4::conv_conv_1` as separate namespaces. Then waited 30+ seconds. Server log: `TTL reaper: pruned empty conversation namespaces count=1` followed by `TTL reaper: entries expired count=1`. Final `/stats`: only `test::4` remained — the conversation namespace was fully pruned.

**Key decisions:**
- **`conv_` prefix is hardcoded.** A conversation_id of `"abc"` and a cache_scope of `"abc"` would otherwise produce identical namespace keys (`"m::3::abc"`), which would silently merge their data. The `conv_` prefix makes collision impossible: `"m::3::abc"` vs `"m::3::conv_abc"`. Operators must avoid scope values starting with `conv_` (documented as a non-negotiable, not enforced server-side).
- **Two-level lookup is two index reads, not one.** A unified "search both namespaces and pick the higher-similarity one" would be cheaper but wrong: it would let a global entry beat a conversation entry on raw similarity, defeating the priority semantics. The cost of two HNSW queries is sub-millisecond; the second only fires on conv-namespace miss.
- **Inserts go to ONE namespace.** No dual-write to "this is conversation-specific AND globally useful" — that's an application-level decision, not a server inference. If the application wants to store an answer in both, it makes two insert calls with different conversation_id values. The brief was emphatic: ferrocache's job is to honour the namespace decision, not to second-guess it.
- **Auto-TTL doesn't override explicit TTL.** A conversation entry with `ttl_seconds: 3600` keeps the 1-hour deadline regardless of `conversation_ttl_seconds`. This preserves the M26 contract: explicit `ttl_seconds` is the user's authoritative signal.
- **Empty conversation namespaces only.** `prune_empty_namespaces` filters by `key.contains("::conv_")` so base/scoped namespaces are never pruned even when empty. Base namespaces typically refill quickly (a transient outage doesn't mean the namespace is dead); pruning them would churn allocations and lose namespace-level stats.
- **Reaper does the rebuild.** Without `rebuild_dirty_namespaces` between `collect_expired` and `prune_empty_namespaces`, the prune predicate would never fire because `collect_expired` populates `evicted_ids`. The brief sketched only `collect_expired → prune`; deviating to add the rebuild was the only way to make the prune actually work in production. The M25 lazy-deletion + 20%-ghost-ratio rebuild trigger means the rebuild is cheap when there's nothing to clear (no-op) and only does real work when the namespace is mostly ghosts — which is exactly the case after a conversation drains.
- **`replay_entry` tombstone branch.** Discovered during the M29 smoke test that the conversation namespace was being re-materialised as a phantom empty entry just after pruning. Root cause: the runtime path (reaper → WAL → flush task → `replay_entry`) had no tombstone branch — it called `insert_with_uuid` with empty/zero defaults. Fix lives in `index.rs` rather than the flush task because (a) `replay_entry` is the one place that owns the WAL→index mapping, (b) startup replay already did the right thing manually, and (c) centralising means any future caller of `replay_entry` (snapshot loader, future bulk-import path) gets the correct semantics for free. Added `entry.tombstone` early-return that calls `remove_by_uuid` and returns `Ok(())` (idempotent — replaying a tombstone for an already-gone UUID is fine).
- **`scope` is a separate response field, not encoded into `id`/`response`.** A caller that wants to know whether the answer is context-specific or general can read `scope` directly. Encoding it into the response body would require parsing. The new field is `Option<String>` so non-conversation queries see no field at all (skip_serializing) — pre-M29 callers parsing the response don't notice the addition.
- **Metric attribution: hits charge their actual namespace, misses charge the base.** A conversation hit increments `ferrocache_namespace_queries{namespace="m::3::conv_x"}`; a global fallback hit increments the base namespace counter; a true miss also charges the base. This makes per-conversation hit rates legible while keeping miss attribution focused on the canonical namespace.
- **State propagation: `conversation_ttl_seconds` lives on `AppState`, not on a config sub-struct.** The brief said "top-level, not nested under cluster". Threading it into AppState mirrors M22's `dead_node_removal_enabled` (also top-level config → state). The handler reads `state.conversation_ttl_seconds` directly without going through `state.cluster.*`.

**Deviations:**
- Brief proposed `prune_empty_namespaces` as a method that returns `()`. Returns `Vec<String>` so the reaper can log the count without re-iterating.
- Brief implied the reaper would call only `prune_empty_namespaces`. Added the explicit `rebuild_dirty_namespaces` call between `collect_expired` and prune (see Key decisions). This required threading `hnsw_config: HnswConfig` into the reaper signature; trivial but worth flagging.
- Brief had `conversation_namespace` and `base_namespace` as separate top-level utilities. Implemented `base_namespace` as a single-line alias for `effective_namespace` so call sites can write `base_namespace(...)` to signal intent ("we're computing the FALLBACK base, not the EFFECTIVE namespace"). Same string output, different contextual reading.
- Brief's `test_two_level_lookup_conversation_hit` and `test_two_level_lookup_global_fallback` were collapsed into HTTP-level server tests (`test_query_with_conversation_id_scope_field` for fallback + `test_query_with_conversation_id_conversation_scope` for direct hit) plus a unit test (`test_conversation_priority_over_global`) that proves the two-level priority order at the index layer. The HTTP tests cover the user-visible behaviour (the `scope` field in the response); the unit test covers the priority semantics. Less duplication, broader coverage.
- Brief sketched test `test_prune_empty_conversation_namespace` calling `prune_empty_namespaces` immediately after `collect_expired`. That fails because `evicted_ids` isn't empty yet. Test now does `collect_expired → rebuild_dirty_namespaces → prune_empty_namespaces` — mirroring the production reaper sequence exactly. Added `test_prune_preserves_conv_namespace_with_evicted_ghosts` to also assert the inverse: a namespace with lingering evicted_ids must NOT be pruned (would lose the ghost-filter set).
- Brief had `test_explicit_ttl_overrides_conversation_ttl` checking with TTLs of 10 and 3600. Tightened the assertion window to ±10s to absorb sub-second clock drift while still proving "explicit TTL = ~3600s, NOT the auto 10s". Same test, less flake-prone.
- Brief sketched 16 tests; shipped 18 (added `test_no_conversation_id_no_fallback` to assert the inverse direction of the priority semantics, and `test_prune_preserves_conv_namespace_with_evicted_ghosts` per above).
- Brief implied the cluster fan-out path should "work by construction". Verified: `forward_query` and `forward_insert` carry the full `req` JSON (which now includes `conversation_id` when set), and each replica computes the same effective namespace → conversation namespace from the same fields. No router changes needed; the integration test suite (32/32) confirms.
- Brief had no fix for the latent tombstone-replay bug. Discovered during M29 smoke test (see Key decisions); fix is in scope because M29's prune is what surfaced it. Documented as the latent-bug-shipped-with-M29 architecture-decisions line.

**Open / next:**
- **No `effective_namespace`-style decompose for `(model_id, scope, conversation_id)`.** Operators reading `/stats` see composite namespace strings like `"all-MiniLM-L6-v2::384::tenant_a::conv_xyz"` — they have to know the convention. A future `/admin/namespaces?decompose=true` could parse and surface the components, but `model_id` itself contains `::` (for the dimension suffix), so the decomposition is heuristic, not bijective. Out of scope.
- **No bulk-conversation invalidate.** `POST /admin/invalidate` works against a single namespace. Killing a whole conversation namespace requires either waiting for TTL or calling DELETE per UUID. A future `DELETE /admin/conversation/:id` could exist but isn't planned for v1.0.
- **Pre-M29 entries don't get auto-TTL retroactively.** `conversation_ttl_seconds` only applies at insert time. Entries inserted before the operator turned the config on stay forever. The reaper's startup expiry pass evicts them only if their per-entry `expires_at` is set — which it isn't for pre-config entries. Documented behaviour, not a bug.
- **Scope value `"conversation"` and `"global"` are user-visible strings.** A future i18n / l10n effort could surface them differently, but the JSON wire field uses these literals. Adding new variants (e.g. `"shared"`) would need a discriminator scheme.
- **Memory cost scales with active conversation count.** Each conversation gets its own HNSW + side-table. For 100K active conversations with 10 entries each, that's 100K HNSW instances. Per-instance overhead is small (a few KB) but the namespace map's HashMap fragmentation could be measurable. The reaper's `prune_empty_namespaces` mitigates by reclaiming dead conversations, but operators with very-long-tail conversation distributions might want a sharded HNSW with conversation_id filtering as a future optimisation. Out of scope for v1.0.
- **No conversation_id support on `/admin/invalidate`.** The brief explicitly skipped it. An operator wanting to "invalidate all entries in conversation X similar to embedding Y" would need to manually compose the namespace. Adding it is straightforward — just thread `conversation_id` through `InvalidateRequest` like `cache_scope` was.
- **`replay_entry` tombstone fix means main.rs has a redundant manual branch.** The startup loop does `if entry.tombstone { remove_by_uuid; continue }` before calling `replay_entry` for non-tombstones. With the new `replay_entry` branch, that explicit guard is technically redundant — `replay_entry` would handle it correctly. Kept the manual branch for now because it's not wrong and removing it would obscure the intent ("startup replays both inserts and removes"). Worth cleaning up in a future pass.

### 2026-05-04 — Mission 30: Phase 7 Integration, Benchmarks, and Documentation
**Built:** v1.0 close-out — validation, benchmarks, and documentation. NO behavioural code changes; this mission ships tests, scripts, and docs only. (1) **Full suite re-baselined.** Ran `cargo test --lib` (222/222), `cargo clippy --all-targets -- -D warnings` (clean), `cargo fmt --check` (clean), `python3 -m unittest discover tests` (59/59), and the docker-compose 3-node integration script (32/32 baseline) — all green from M29 with no regressions. (2) **`tests/simulate.py --ttl <secs>` flag.** New optional CLI flag; when set, every Phase 1 insert carries `ttl_seconds=<ttl>`. The results banner now shows `TTL: <n>s (per-insert ttl_seconds, M26)` when active. Lets the realistic FAQ workload double as an inline-expiry smoke test against a live binary. (3) **Phase 7 integration test block (Test 9).** Added 12 new assertions to `tests/cluster_integration.sh` covering all M24–M29 features against the live 3-node docker-compose cluster: TTL insert returns ok+UUID, cross-tenant scope miss + same-tenant scope hit, conversation-namespace hit reports `scope=conversation`, conversation-fallback hit reports `scope=global`, `DELETE /entry/:uuid` cluster fan-out + cross-cluster miss, `/admin/invalidate` finds 1 radius match, M27 exact-match pre-filter fires (orthogonal embedding + identical normalized text → `exact_match=true`), TTL-expired entry returns miss after 8s sleep at threshold 0.99 (tight threshold avoids accidental match against test 10's [0.3,0.3,0.3,0.3] entry which has cosine ≈ 0.91 to the TTL probe), and `/admin/entry-stats` returns 200. Used `?local=true` on insert + invalidate + exact-match-query so tests don't depend on which replicas the ring picks. The cluster integration suite now reports **44/44** (was 32/32). (4) **Eviction throughput baseline.** Ran `bench_concurrent.py` at concurrency 100 with and without `FERROCACHE_HNSW__MAX_ENTRIES_PER_NAMESPACE=5000`. Without eviction: insert 1900/s, p99 70ms. With eviction (5K cap forcing rebuild on every batch): insert 1313/s, p99 1824ms. Throughput drop: ~31%, just over the brief's 30% threshold; investigation confirmed the cause is periodic HNSW rebuild under the index write lock (the M25 design — 20% ghost-ratio threshold), which is the documented correctness/performance trade-off. Numbers recorded in the README's Eviction Overhead table. (5) **`tests/benchmark_vs_gptcache.py`.** New benchmark script comparing ferrocache (single-node, HTTP) vs GPTCache (in-process Python) on the same synthetic workload (200 seeds × 3 paraphrases + 50 unrelated = 750 queries against 200 inserts). Both systems use sentence-transformers `all-MiniLM-L6-v2` for embeddings — apples-to-apples. **GPTCache subprocess isolation:** GPTCache pulls in `faiss-cpu` + `onnxruntime` which currently lack wheels for Python 3.13+; on the Python 3.14 test box, the child process SIGSEGVs at faiss init. The script runs GPTCache via `subprocess.run([python, __file__, "--gptcache-worker"], ...)` so the segfault doesn't propagate; on failure the table reports `N/A` for GPTCache columns and the rest of the report continues. ferrocache numbers from the actual run: hit rate 99.8%, 0 false hits, query p50 0.44ms p99 0.84ms (HTTP round-trip), insert p50 8ms p99 11ms (WAL fsync), RSS 14.1 MB after 200 inserts, insert throughput 2476/s @ concurrency 50. **Manual GPTCache config:** the worker bypasses GPTCache's `init_similar_cache` (which defaults to ONNX) and constructs `Cache.init` with the shared sentence-transformers model as `embedding_func`, plus a `pre_func` that overrides the default `last_content` (which expects an OpenAI-style `messages` dict — the benchmark passes plain strings). (6) **Makefile target `benchmark-vs-gptcache`.** Best-effort `pip install -q sentence-transformers gptcache psutil` followed by the script. Optional deps; the script handles missing imports gracefully. (7) **README v1.0 rewrite.** The previous README was last substantively touched at M8 with incremental additions through M23. Replaced with a complete v1.0 rewrite: feature list extended to all of Phase 7, full API table (every endpoint), full configuration table (all env vars including `max_entries_per_namespace`/`expire_scan_interval_secs`/`conversation_ttl_seconds`), new "Eviction and Expiry" section explaining the four-path lifecycle (LRU / TTL / DELETE / invalidate) and the WAL-tombstone durability guarantee, new "Tenant Isolation" section with a `wrap_openai(..., cache_scope=...)` example, new "Multi-Turn Conversations" section explaining two-level lookup with the `scope` response field, updated benchmark tables with **real measured numbers** from the M30 runs (10.4× group-commit speedup at concurrency 100; eviction overhead table; GPTCache comparison table with N/A noted), production checklist as a checkbox list, framework integration sections updated to show `cache_scope`/`conversation_id` kwargs. (8) **`clients/ferrocache-skill.md` complete rewrite.** The skill file (LLM-agent reference) was M19's Phase-5 snapshot — significantly out of date. Replaced with a v1.0 rewrite: every endpoint (including the M26 DELETE/invalidate, M27 exact-match, M28 scope, M29 conversation), every request/response field, complete config env-var tables (top-level, HNSW, cluster), production checklist with critical alert thresholds, troubleshooting runbook covering "entries returning after restart", "high eviction rate", "conversation namespace memory growth", "exact-match not firing", "502 upstream", "dimension mismatch", and tenant/conversation leakage scenarios. The troubleshooting entry for "entries returning after restart" explicitly references the M29 `replay_entry` tombstone-branch fix as a known-fixed bug. (9) **Claude.md final update.** Phase line: "Phase 7 complete. v1.0 ready. 30 missions across 7 phases." Dropped M28 rolling log; added M28+M29+M30 one-liners to Completed work. Added 3 architecture decisions (Phase 7 validation, GPTCache subprocess isolation, ferrocache-skill.md rewrite). M29 log retained as the only rolling log alongside this M30 entry — that's the spec's "last 2".

**Verified:**
- `cargo build --release` — clean.
- `cargo test --lib` — 222/222 pass. No new Rust unit tests this mission (validation only).
- `cargo clippy --all-targets -- -D warnings` — clean.
- `cargo fmt --check` — clean.
- `python3 -m unittest discover tests` — 59/59 pass. No new Python tests this mission (validation only).
- `docker compose up -d --build && sleep 7 && ./tests/cluster_integration.sh && docker compose down -v` — **44/44** pass on a fresh cluster (was 32/32 before; +12 Phase 7 assertions in the new Test 9 block).
- `make benchmark-vs-gptcache` — runs end-to-end. ferrocache columns populated with real numbers; GPTCache columns gracefully report N/A on Python 3.14 due to the faiss/onnx native-segfault subprocess crash, which the harness catches without propagating.
- `bench_concurrent.py --concurrency 100 --duration 5` with and without eviction — recorded both throughput numbers in the README.
- `python3 -c 'import ast; ast.parse(open("tests/simulate.py").read())'` — syntax ok after `--ttl` plumbing.

**Key decisions:**
- **No new Rust features.** The brief was emphatic that M30 is validation + documentation only. The `simulate.py --ttl` flag is the single sanctioned exception (and it's a Python harness, not server code). Any "while we're here" temptation (e.g. an `?include_scope=false` query knob, a `/admin/conversations` endpoint) was deferred to post-1.0.
- **Subprocess isolation for GPTCache.** A naive `try/except` around the GPTCache call won't catch a SIGSEGV — the child process dies with the parent. Wrapping the GPTCache run in `subprocess.run` lets us treat a segfaulting child as a soft failure, print N/A, and continue with ferrocache numbers. This makes the benchmark portable across Python versions: works on 3.10–3.12 with full numbers, works on 3.13+ with ferrocache-only numbers.
- **`?local=true` on Phase 7 integration tests.** Tests 9 (invalidate), 10 (exact-match), and parts of test 11 (TTL probe) use `?local=true` so they don't depend on which replicas the ring happens to assign for the test's embedding. Without it, an entry replicated to nodes A+B and an invalidate sent to node C found 0 local matches and the test failed nondeterministically. The brief's example didn't use `?local=true`; the deviation is necessary because the actual ring assignment varies with embedding contents and the test harness shouldn't have to know about it.
- **TTL probe threshold 0.99.** The test 11 TTL miss check uses threshold 0.99 instead of 0.90. With multiple test entries in the namespace from earlier asserts (orthogonal-ish but not orthogonal-enough), threshold 0.90 was matching the M27 exact-match entry's `[0.3,0.3,0.3,0.3]` vector with cosine ≈ 0.91 to the TTL probe's `[0.1,0.2,0.3,0.4]`. Tightening to 0.99 makes the TTL assertion specific to the actual TTL entry, not "any nearby vector".
- **README's eviction-overhead row goes IN the public benchmarks.** The brief said to "investigate" if throughput dropped > 30% — investigation confirmed the cause is M25's design choice (inline rebuild for graph quality), not a regression. Documenting the trade-off honestly in the README is more useful than hiding it; operators tuning `max_entries_per_namespace` need to see that there's a real cost.
- **GPTCache `pre_embedding_func` override.** GPTCache's default `last_content` expects an OpenAI-style `{"messages": [...]}` dict; the benchmark passes plain strings via `cache_put(prompt, answer)`. Without overriding `pre_embedding_func`, the first put crashes with `'NoneType' object is not subscriptable`. The override is `lambda data, **_: data["prompt"]` — the kwargs pipeline forwards the user data verbatim under the `prompt` key.
- **`replay_entry` redundant manual branch retained in main.rs.** M29's bug fix moved tombstone handling INTO `replay_entry`, but main.rs's startup loop still has its own `if entry.tombstone { remove_by_uuid; continue }` branch from M25. With the M29 fix, that branch is technically redundant. M30 didn't remove it because (a) it's not wrong, (b) it makes startup intent explicit at the call site ("we replay both inserts and tombstones, here's the tombstone path"), and (c) removing it now risks a regression for v1.0. Future-cleanup candidate.
- **No new tests for M30 itself.** The mission's verification IS the test list (44 integration assertions, 222 unit tests, 59 Python tests). Adding "test_m30_documentation_exists" or similar would be ceremony. The integration test block is itself the regression guard against future breakage of any individual Phase 7 feature.
- **Skill-file structure mirrors the README, not the codebase.** The skill file is for LLM agents, not developers reading source — so it's organised by "what would I need to know to use this?" (endpoint reference, lifecycle, isolation, troubleshooting) rather than by mission number or module structure. The Completed-work list in Claude.md is the per-mission map; the skill file is the operator-eye-view.

**Deviations:**
- Brief sketched 12 new integration test assertions; shipped 13 (added `/admin/entry-stats` returns 200 as a smoke check — it's mentioned in the API table but had no integration coverage).
- Brief proposed a single TTL-insert assertion ("TTL insert returns UUID"). Split into two: "TTL insert returns ok" + "TTL insert returns non-empty UUID". The first guards JSON shape, the second guards UUID generation. Cleaner failure messages when one fails.
- Brief had `EM_HIT=$(... | jq -r '.exact_match')` as the assertion key; renamed to `EM_FLAG` and asserted against `"true"` because `jq -r` of a JSON boolean produces the string `"true"`/`"false"`, not an exit code. Same outcome, clearer reading.
- Brief's GPTCache benchmark section assumed `gptcache` works out of the box. On Python 3.14 it segfaults; the script handles this gracefully (subprocess) and the README/skill file both note "Python ≤ 3.12 recommended for GPTCache comparison". The illustrative numbers in the brief are replaced with N/A on this environment with a clear footnote, because using the brief's placeholder numbers would be presenting unmeasured data as measured — a bigger documentation sin than admitting the comparison didn't run.
- Brief's "Update the configuration table" rows were all that was asked; the README rewrite extended it to include retry, phi, dead-node-removal, read-repair, and TLS rows that were already valid pre-M30 but missing from the table. The README is now the complete v1.0 reference, not the M28-snapshot reference.
- Brief sketched a Markdown table with sample numbers (e.g. "Hit rate 100% / 97.2%"). Shipped real numbers from the M30 runs; ferrocache hit rate is 99.8% (1 of 600 paraphrase queries fell below threshold 0.90), not 100% — the synthetic paraphrase workload is harder than the prior FAQ simulation because of its phrasing breadth.

**Open / next:**
- **GPTCache numbers on Python ≤ 3.12.** The benchmark script is portable; running it on a Python 3.10/3.11/3.12 box with `pip install gptcache faiss-cpu onnxruntime sentence-transformers` would produce real GPTCache columns. Out of scope for this mission's deliverables (the box has only 3.14); operators can re-run the script in a 3.12 venv to populate the comparison.
- **Eviction throughput at the boundary.** 31% drop is right at the brief's 30% threshold. Future mitigations: incremental rebuild (rebuild a slice per batch instead of all-at-once), background rebuild on a separate task with an atomic snapshot swap, or tighter ghost-ratio bounds (15% instead of 20%) to spread rebuild work. None planned for v1.0; the trade-off is acceptable for a cache-tier service.
- **Subprocess isolation pattern.** The benchmark's `--gptcache-worker` mode is the only place we use Python subprocess for crash isolation. If we add more native-extension comparisons (Faiss directly, Milvus, ScaNN), the same pattern would apply — could be extracted to a shared helper.
- **Skill file vs README divergence risk.** Two long-form documents now describe the API. Future API additions need to update both. Could be addressed by generating one from the other (e.g. README's API table is the source of truth, skill file is generated). Out of scope for v1.0; for now, both are hand-written and the M30 commit pairs them so they don't diverge silently.
- **No `simulate.py` integration test.** The `--ttl` flag exercises the inline expiry path on a live binary, but it's not in the docker-compose integration script — it's a manual smoke test. Could be wired into the integration script with a longer TTL and a reaper interval of 1s, but the existing Test 9 block already covers TTL via `/insert ttl_seconds=3` + `/query` after 8s. Redundant.
- **Module map in Claude.md is now ~1100 chars on the index.rs line.** It accumulates feature mentions from each mission. At some point it'd be useful to break it into a per-file `docs/architecture.md`. Not for v1.0; the Claude.md "evergreen" section still fits in the auto-loaded prompt window.
- **v1.0 release tag.** With M30 done, ferrocache is ready to tag. The release CI on `v*` tags publishes a Docker image to GHCR + a Python package to PyPI via OIDC trusted publishing (M13). Publishing is the operator's call; this mission stops at "ready to tag".
