# ferrocache — Project Context

## Section 1: Evergreen

**What this is:** Distributed semantic cache for LLM applications, written in Rust. Single binary, multi-node via consistent hashing + gossip replication. Portfolio project for big-tech Core SWE interviews.

**Current phase:** Phase 7 in progress. M24 done (access tracking). Next: M25 — LRU eviction.

**Completed work:**
- M1: axum scaffold (3 routes, tracing, env-based port)
- M2: hnsw_rs integration (DistCosine, side-table keyed by usize, dim lock)
- M3: WAL (NDJSON, fsync-per-insert, replay on startup, corrupt-line skip; WAL-first insert path; UUIDs stable across restarts)
- M4: config crate (TOML + env merge), request validation (4096 dim, 100KB resp, threshold range), /stats endpoint, dead-code cleanup
- M5: consistent hash ring (FNV-1a, 64 vnodes default) + chitchat gossip discovery, /cluster/status endpoint, single-node fallback
- M6: cluster-aware /query routing + /insert synchronous replication via reqwest, `?local=true` loop-prevention, coordinator UUID stamping, get_n_nodes replica walk
- M7: Dockerfile + docker-compose 3-node cluster + bash integration tests; fixed env-var `prefix_separator` config bug (latent since M4)
- M8: README + Mermaid diagram, criterion benchmarks (real numbers), stdlib-only Python client, GitHub Actions CI (check + integration), library/binary split for bench imports
- M9: simulation harness (`tests/simulate.py` + `simulate_no_ml.py`) — FAQ workload with sentence-transformers, 100% hit rate / 0 false positives at 0.90 threshold
- M10: drop-in SDK middleware (`wrap_openai`, `wrap_anthropic`) with proxy attribute delegation, fail-open on cache outage, env-var config; restructured Python client into a package
- M11: framework backends — `FerrocacheCache` (LangChain `BaseCache`) + `FerrocacheLLM` (LlamaIndex `CustomLLM` subclass); optional imports, fail-open default, custom `embed_fn` supported
- M12: MCP server (`ferrocache.mcp_server`) over stdio — 3 tools (`semantic_cache_lookup`, `semantic_cache_store`, `cache_status`), text-in/JSON-out (embedding handled internally), Claude Desktop + Claude Code setup docs
- M13: distribution — `pyproject.toml` with optional extras (`[openai]`, `[anthropic]`, `[langchain]`, `[llamaindex]`, `[mcp]`, `[all]`); base `pip install ferrocache` is zero-deps; root + Python LICENSE; release CI on `v*` tags publishes Docker image to GHCR and Python package to PyPI via OIDC trusted publishing
- M14: namespace partitioning — `SemanticIndex` is a `HashMap<model_id → NamespacedIndex>`, each with its own HNSW + side-table; `model_id` required on `/insert` and `/query`; Python integrations auto-derive `model_id` from default embed model; PyPI bumped to 0.2.0; pre-M14 WAL entries migrate to `legacy::unknown`
- M15: WAL compaction + snapshotting — `src/snapshot.rs` writes `magic+version+wal_seq+bincode(Vec<SnapshotEntry>)` atomically; WAL gained monotonic `sequence` numbers; startup loads snapshot then replays only `seq > watermark`; auto-compaction every 10K inserts (configurable) + manual `POST /admin/compact`; `CacheEntry` re-stores `embedding`+`query_text`
- M16: Prometheus `/metrics` (hand-written text exposition, no `prometheus` crate) — atomic counters with per-namespace `RwLock<HashMap>`, fixed 16-bucket `LatencyHistogram` (100µs → 10s), `/stats` JSON gains `counters` block, monitoring compose overlay (Prometheus + Grafana on :9090/:3100) with provisioned datasource and 8-panel ferrocache dashboard
- M17: bearer token auth — opt-in via `FERROCACHE_AUTH_TOKEN`; `subtle::ConstantTimeEq` compare; `/health` and `/metrics` exempt; inter-node forwards carry the same token; Python client reads env automatically; integrations gain `auth_token=` kwarg
- M18: cluster mTLS — opt-in via `FERROCACHE_CLUSTER__TLS__ENABLED`; rcgen 0.13 + rustls 0.23; second TLS listener on `internal_port` (default `port + 1000`) requires client cert chained to cluster CA; `WebPkiClientVerifier` rejects anonymous clients at handshake; `gen_certs` binary writes dev CA + per-node cert/key; production loads PEMs from `cluster.tls.{ca_cert,node_cert,node_key}_path`
- M19: replication retry (exponential backoff + ±20% jitter, retries on connect/timeout/5xx, never 4xx) + security audit (scrubbed peer IDs / WAL paths / snapshot paths from HTTP error bodies, kept in tracing logs) + `docs/security.md` threat-model/deployment-recipes/known-limitations + `clients/ferrocache-skill.md` LLM reference; `replication_retries_total` metric distinct from `replication_failures_total`
- M20: WAL group-commit — inserts push `WalCommand::Insert` to mpsc channel, single background flush task batches up to `wal_batch_size` (default 256) requests within `wal_batch_timeout_ms` (default 1ms), one fsync per batch; flush task is sole writer (compaction goes through same channel); 7.5× insert throughput at concurrency 100; new `tests/bench_concurrent.py` harness
- M21: phi accrual failure detector — `src/failure_detector.rs` with `PhiAccrualDetector` (sliding window of inter-arrivals, normal CDF via Abramowitz & Stegun erf, `phi = -log10(1 - F(elapsed))`), `PeerStatus::{Alive, Suspected, Dead}` state machine; heartbeats piggyback on chitchat reconciler; `peer_health` map in `/cluster/status`, `ferrocache_peer_phi` gauge in `/metrics`; soft routing skips Dead peers (query → miss instead of 502, insert fan-out → degraded replication)
- M22: ring reassignment + failover — reconciler decoupled from chitchat liveness (only phi-accrual `Dead` triggers ring removal; `Suspected` stays); `dead_nodes: HashSet<String>` on `ClusterState` surfaced via `/cluster/status.dead_nodes`; re-joining nodes added back to ring AND removed from dead set on the same tick; zero data movement (clockwise successor already has data via replication_factor=2); `dead_node_removal_enabled` flag; `ferrocache_ring_changes_total` counter
- M23: read repair — coordinator fan-outs to non-dead replicas on miss, returns first hit, spawns async repair via `/internal/entry/{uuid}` + WAL group-commit; `LocalParam.repair: Option<bool>` three-state (none/forwarded/fan-out); UUID reverse lookup in `SemanticIndex`; `read_repair_enabled` config flag; `read_repairs_total` + `read_repair_failures_total` counters; eventually-consistent healing through traffic, no Merkle trees

**Module map:**
- lib.rs — re-exports modules so benches and external consumers can import
- main.rs — entry, tracing init, config load, WAL replay, optional cluster init, serve
- server.rs — router, handlers, request validation, routing/replication coordination, tests
- index.rs — `SemanticIndex` (hnsw_rs wrapper + HashMap side-table), `replay_entry`/`replay_snapshot_entry`; per-namespace `uuid_to_internal: HashMap<String, usize>` plus top-level `uuid_to_namespace: HashMap<String, String>` so `get_entry_by_uuid -> Option<FullEntry>` is O(1) (used by /internal/entry/{uuid} for read repair); insert path de-dupes on UUID so re-inserting the same entry is idempotent; `CacheEntry` carries `inserted_at`/`last_accessed_at`/`access_count` (M24); `record_access(model_id, internal_id)` bumps the latter two under the index write lock; `top_entries_per_namespace(N)` powers `/admin/entry-stats`; `now_unix_secs()` helper exported for stamping `inserted_at` at the insert site
- wal.rs — `Wal` (append, fsync, sequence, truncate, replay) + `GroupCommitWal` (mpsc-fed flush task that batches writes and fsyncs once per batch); `WalCommand::{Insert,Compact}`, `InsertError::{Wal,Index}`, `GroupCommitConfig`; flush task owns the only `Wal` handle and the index write lock
- models.rs — request/response DTOs (InsertRequest carries optional `uuid`)
- config.rs — FerrocacheConfig (port, wal_path, hnsw, cluster, compact_interval_inserts, auth_token, wal_batch_size, wal_batch_timeout_ms), HnswConfig, ClusterConfig (gossip_addr, api_addr, replication_factor, seed_nodes, tls, max_replication_retries)
- state.rs — AppState { node_id, index, wal: GroupCommitWal, wal_path, snapshot_path, hnsw_config, cluster, router, replication_factor, metrics, auth_token, max_replication_retries }
- auth.rs — `auth_middleware` (axum `from_fn_with_state`); constant-time `subtle::ConstantTimeEq`; `/health` + `/metrics` exempt; only installed when `auth_token` is non-empty
- tls.rs — `TlsBundle`, `generate_ca`/`generate_node_cert` (rcgen 0.13), `load_or_generate` (disk vs in-memory dev), `build_server_config` (rustls 0.23 mTLS — `WebPkiClientVerifier` requires a client cert chained to the cluster CA), `install_default_crypto_provider` (idempotent aws-lc-rs install); used by `main.rs` for the internal listener and `router.rs` for the reqwest identity
- bin/gen_certs.rs — dev-cert generator binary (`cargo run --bin gen_certs [node_ids...]`); writes `./certs/ca.pem` + `./certs/<node_id>/{cert,key}.pem`; intentionally not built into the production Docker image
- ring.rs — HashRing (BTreeMap<u64, node_id>, FNV-1a, virtual nodes, get_n_nodes for replica walk)
- cluster.rs — ClusterState wrapping chitchat; `reconcile_step` (extracted free fn so tests drive it without UDP) feeds the phi accrual detector then drives ring membership: ring ADDs come from chitchat snapshot vs ring-set diff (with re-join detection vs `dead_nodes`), ring REMOVALS come from detector `Dead` status (M22), never from chitchat `live_nodes` directly; `dead_nodes: HashSet<String>` tracks ring-removed peers for `/cluster/status`
- failure_detector.rs — `PhiAccrualDetector` (sliding window of inter-arrival times per peer, normal CDF via Abramowitz & Stegun erf, phi=-log10(1-F(elapsed))); `PeerStatus::{Alive, Suspected, Dead}`; injectable `_at(Instant)` methods so tests don't depend on wall-clock; passive — never sends heartbeats, only observes
- router.rs — ClusterRouter (reqwest client) — forward_query / forward_insert return typed `reqwest::Result` so the `forward_with_retry` helper can classify failures (retry on connect errors / timeouts / 5xx; never on 4xx); ±20% jitter from `rand::thread_rng()`; metric `ferrocache_replication_retries_total` increments inside the helper
- snapshot.rs — `SnapshotEntry` + `write_snapshot`/`read_snapshot` (magic+version+wal_seq+bincode); `compact()` helper that writes the snapshot and truncates the WAL atomically
- metrics.rs — `Metrics` (atomic counters + per-namespace `RwLock<HashMap<NamespaceMetrics>>`), `LatencyHistogram` (16 fixed buckets 100µs–10s), `render()` to Prometheus text exposition format
- benches/cache_bench.rs — criterion: insert / query_hit / query_miss / insert_with_wal / snapshot_write_10k
- tests/bench_concurrent.py — concurrent throughput harness (HTTP via FerrocacheClient at concurrency 1/10/50/100; insert + query_hit + query_miss); `make bench-concurrent`
- clients/python/ferrocache.py — stdlib-only Python client (urllib + json)

**Architecture decisions (append-only, one line each):**
- Client computes embeddings externally; ferrocache stores/compares f32 vectors
- Crates: tokio, axum 0.7, hnsw_rs, chitchat 0.10, tracing, serde_json, config, reqwest
- WAL format: newline-delimited JSON, replayed on startup, fsync per insert
- Consistent hashing key: u64 from first 8 bytes of embedding (first 2 f32 values, big-endian)
- Cosine distance: hnsw_rs DistCosine returns 1-similarity; we convert
- Side-table: HashMap<usize, CacheEntry> keyed by hnsw internal id; UUID inside CacheEntry
- WAL is source of truth; production insert path is WAL-first then replay_entry
- insert() (UUID-generating) is #[cfg(test)] only; production uses replay_entry
- Config priority: env vars (FERROCACHE_ prefix, `_` prefix-sep, `__` section-sep, `,` list-sep) > ferrocache.toml > defaults
- tokio::sync::RwLock for index; WAL is owned by a single background flush task (no shared mutex)
- Validation limits (server.rs constants): MAX_EMBEDDING_DIM=4096, MAX_RESPONSE_BYTES=102_400
- Threshold accepted range on /query: 0.0..=1.0
- Hash ring: FNV-1a (cross-process deterministic), 64 virtual nodes per physical node by default
- Cluster mode is opt-in via `cluster.enabled`; `false` keeps Phase 1 behavior bit-identical
- Ring reconciler runs every 2s in a background task; chitchat gossip interval = 1s
- Each node advertises its API addr via chitchat KV under key `api_addr`; reconciler reads peers' addrs
- Routing: `?local=true` skips ring lookup (used by forwarded requests + debugging); coordinator stamps UUID before fan-out
- Replication: synchronous, primary-or-coordinator + (replication_factor-1) replicas; any replica failure → 502
- Forward failures return 502 (BAD_GATEWAY) to distinguish "peer failed" from local 500
- Docker runtime image needs `libgomp1` (hnsw_rs → rayon → OpenMP)
- `seed_nodes` is parsed from a comma-separated env string via config-rs `list_separator(",")`+`with_list_parse_key`
- `Wal::open` mkdir's parent dir so containers can mount an empty `/data` volume
- Crate is library + binary (lib.rs re-exports modules); `cargo build --release --bin ferrocache` for prod images
- Benchmarks via criterion; Python client uses only stdlib (urllib+json)
- Python package on PyPI with optional extras; Docker image on GHCR; release CI on version tags
- Index is namespace-partitioned by `model_id`; each namespace has its own HNSW instance + side-table
- `model_id` is required on `/insert` and `/query`; old WAL entries without it default to `legacy::unknown`
- `model_id` format convention: `model_name::dimension` (e.g. `all-MiniLM-L6-v2::384`)
- Cross-namespace queries are impossible by construction — vectors from different models never compare
- Snapshot format: `magic (FERROSNA) + version (u64=1) + wal_sequence (u64) + entry_count (u64) + bincode-encoded Vec<SnapshotEntry>`; written atomically via temp+rename so a crash never corrupts the prior snapshot
- Compaction: snapshot the side-table entries, then truncate the WAL — HNSW is rebuilt from stored embeddings on load (no need to serialize the graph itself)
- WAL entries carry a monotonic `sequence: u64` that never resets — startup skips entries `<= snapshot_watermark` so the WAL tail replay is bounded
- Auto-compaction every N inserts (default 10K via `compact_interval_inserts`, 0 disables); manual via `POST /admin/compact`
- `CacheEntry` again stores `embedding` + `query_text` so snapshots can dump the full record (re-added in M15; was dropped in M4)
- Prometheus `/metrics` is hand-written (no `prometheus`/`metrics` crate); atomic counters with `Ordering::Relaxed` + a fixed-bucket `LatencyHistogram`
- Per-namespace hit/miss/insert counters carry a `namespace` label; global query/insert latency histograms have 16 buckets (100µs → 10s) plus a `+Inf` bucket
- `/stats` JSON gained a `counters` object so users without Prometheus can still read the same numbers
- Monitoring stack lives in `monitoring/` as a docker-compose overlay (`-f docker-compose.yml -f monitoring/docker-compose.monitoring.yml`); base cluster runs unchanged without it
- `CacheEntry` again stores `embedding` + `query_text` so snapshots can dump the full record (re-added in M15; was dropped in M4)
- Bearer token auth via `FERROCACHE_AUTH_TOKEN` env var; disabled when unset (opt-in, backward compatible)
- Auth exempts `/health` and `/metrics` (load balancers and Prometheus need unauthenticated access)
- Token comparison uses `subtle::ConstantTimeEq` to prevent timing attacks; the token value is never logged at any level
- Inter-node replication forwards include the same bearer token (shared-secret cluster model — all nodes carry the same `FERROCACHE_AUTH_TOKEN`)
- mTLS between cluster nodes via rustls 0.23 + rcgen 0.13; opt-in via `cluster.tls.enabled`; second TLS listener on `internal_port` (default `port + 1000`) serves the same router; public port stays plain HTTP
- Dev mode (no cert paths) auto-generates a self-signed CA + leaf in memory; production loads PEMs from `cluster.tls.{ca_cert,node_cert,node_key}_path`
- TLS reqwest client trusts ONLY the cluster CA (system roots disabled) and presents the node leaf as client identity — i.e. mTLS, not just TLS
- Gossip (chitchat UDP) remains unencrypted: it carries only ring metadata (node IDs + forwarding addresses), not cached data
- `cargo run --bin gen_certs [node_ids...]` writes `./certs/ca.pem` + `./certs/<node>/{cert,key}.pem` for local clusters; the production Dockerfile builds only `--bin ferrocache`, so `gen_certs` never ships in images
- Replication retry: exponential backoff (50ms × 2^attempt, ±20% jitter, capped at 5s), max 3 attempts by default; retries on connect errors + timeouts + 5xx only, never on 4xx (deterministic failures)
- `ferrocache_replication_retries_total` metric tracks retry pressure separately from final failures — retries that succeed do NOT count as failures, so dashboards can distinguish "noisy network, eventually succeeded" from "peer permanently down"
- WAL group-commit (M20): inserts push `WalCommand::Insert` to an mpsc channel; a single background flush task owns the `Wal`, batches up to `wal_batch_size` requests (default 256) or `wal_batch_timeout_ms` (default 1ms) elapses, writes the entire batch in one `write(2)` and one `fsync(2)`, then takes the index write lock once and inserts every entry; per-request oneshot replies fire after the lock is released. `wal_batch_size = 1` degrades to per-insert fsync (pre-M20 behavior) and is what tests use.
- Insert throughput scales with concurrency under group-commit (measured: 7.5× at concurrency 100 vs per-insert fsync); single-client insert is slightly slower (batch timeout adds ~1ms wait); query throughput is unaffected (read path doesn't fsync)
- The flush task is the SOLE writer to the WAL — `compact` is also a `WalCommand` so it serializes with insert flushes (FIFO via the same channel); auto-compaction's `inserts_since_compact` counter lives in the flush task only (no atomics, no locking, single producer)
- Insert handler error surface: `InsertError::Wal` → 500 "persistence failed" (paths/error chain stay in logs), `InsertError::Index` → 500 with the underlying message (e.g. "dimension mismatch") preserving M19 audit shape
- Phi accrual failure detector per peer: sliding window of heartbeat inter-arrivals (default 100 samples), normal CDF, phi = -log10(1-F(elapsed)); threshold 8.0 default (Cassandra/Akka standard); std-dev floored at 100ms so a perfectly regular heartbeat stream still has a defined CDF
- Heartbeats piggybacked on chitchat gossip — the existing 2s reconciler tick records a heartbeat for each peer chitchat reports as live, then runs `check_all`; no separate heartbeat task
- Three peer states: `Alive → Suspected (phi ≥ threshold) → Dead (phi ≥ 2×threshold)`; recovery to `Alive` on any new heartbeat (Dead is not permanent — ring removal is M22's job)
- Dead peers skipped in query routing (immediate miss instead of retry-then-502) and insert fan-out (degraded replication with warn log naming `dead_peers` and `effective_replicas`); soft optimization, retry logic still runs if a "dead" peer races back into a request
- Per-peer phi exposed as `ferrocache_peer_phi{peer=...}` gauge plus `ferrocache_peers_suspected` / `ferrocache_peers_dead` gauges in /metrics; `peer_health` map in /cluster/status JSON
- Ring membership decoupled from chitchat liveness (M22): ring removals driven exclusively by phi accrual `Dead` status; `Suspected` peers stay in the ring (might recover); `chitchat.live_nodes()` controls discovery + KV propagation only
- Dead nodes tracked in `dead_nodes: HashSet<String>` on `ClusterState`, surfaced via `/cluster/status.dead_nodes`; re-joining nodes (reappear in chitchat snapshot) are added back to ring AND removed from the dead set on the same reconcile tick
- Ring reassignment is automatic and zero-data-movement: removing a dead node's virtual nodes from the `BTreeMap`-backed `HashRing` makes its arc fold into the clockwise successor, which is already a replica thanks to `replication_factor=2` — the cache property (no durable single-source-of-truth contract) means we don't need anti-entropy to ship data; missed entries are recomputed by the LLM on the next miss
- `failure_detector.remove_peer` is NOT called from the reconciler — the detector keeps history across chitchat-driven drops so phi can keep climbing into Dead even after gossip stops reporting the peer; only when an operator actually removes a node from the cluster would `remove_peer` apply (out of scope)
- Re-joined nodes serve stale data until the next anti-entropy pass (M23 will add this); stale misses are acceptable in cache semantics — the client re-calls the LLM, the response is cached on the new owner, ring is consistent again
- `dead_node_removal_enabled` config flag (default `true`) supports a monitoring-only mode where phi reports without ring mutation — useful for canary rollouts of the failover behaviour
- Ring change observability: `ferrocache_ring_changes_total` counter (every add or remove from the reconciler) + `ferrocache_ring_members` gauge mirror what's in the ring; bumped from `Metrics::record_ring_change()` inside `reconcile_step`
- Read repair on query miss (M23): coordinator (= ring owner that received the forwarded query) queries non-dead replicas in parallel via `futures::future::join_all`, returns first hit to client immediately, spawns `tokio::spawn` background task that fetches full entry via `/internal/entry/{uuid}` then re-inserts locally through the WAL group-commit channel (full durability); failures (network, 404, channel closed) increment `read_repair_failures_total` but never crash
- `/internal/entry/{uuid}` returns `FullEntryResponse { uuid, embedding, response, query_text, model_id }` — embedding is required so the coordinator can re-insert; 404 maps to `Ok(None)` in `forward_get_entry`; `/internal/read-repair` is a UUID-mandatory `/insert` variant going through the same WAL channel
- `LocalParam.repair: Option<bool>` semantics: `None` defaults to "repair if NOT explicitly local-only" (so client `?local=true` skips repair, but the coordinator's forwarded `?local=true&repair=true` triggers it); `Some(false)` set by the read-repair fan-out itself prevents recursion; `Some(true)` set by the coordinator's `forward_query` makes the recipient know it's the canonical primary
- UUID reverse lookup populated on every `replay_entry`/`replay_snapshot_entry` (not just new inserts) — old WAL/snapshot replay paths also benefit from O(1) uuid lookup
- Read repair is eventually consistent — re-joined nodes heal as traffic naturally queries their stale arcs; entries never queried again are never repaired (acceptable for a cache, since a forgotten entry just means the LLM gets called again on its next visit)
- No Merkle trees, no background reconciliation, no bulk transfer — read repair only, one entry at a time. Phase 6 spec deliberately picked this over the more complex Cassandra-style anti-entropy because the cache semantics make eventual healing through traffic perfectly correct.
- `read_repair_enabled` config flag (default `true`) gates the entire fan-out — when false, a coordinator miss returns immediately without touching replicas; phi monitoring still runs
- `CacheEntry` tracks `inserted_at` (set once, persisted in WAL), `last_accessed_at` (updated in-memory on every hit, persisted in snapshots), `access_count` (same)
- Access metadata is soft state: WAL stores only `inserted_at`; `last_accessed_at` and `access_count` are snapshot-persisted but lost on crash (rebuild naturally via traffic) — the alternative (a WAL write per query hit) would defeat the cache
- Query hits take a brief index write lock for metadata update (HashMap lookup + 2 field writes, sub-µs); misses stay read-only
- `/admin/entry-stats` exposes top-10 most-accessed entries per namespace for ops visibility — O(n log n) sort per namespace, fine for an admin endpoint
- `QueryHit` carries `internal_id: usize` so the handler can call `record_access` without a second HNSW or HashMap lookup
- Pre-M24 WAL entries lack `inserted_at` and deserialize to 0; pre-M24 snapshots default the three new fields to 0 via `#[serde(default)]` — entries with `inserted_at == 0` are treated as "oldest" by future LRU (M25), the correct behaviour for entries from before access tracking

**Non-negotiable constraints:**
- Auth (bearer token) and cluster mTLS are both opt-in; public-port TLS handled by reverse proxy
- No UI
- No direct OpenAI/Anthropic API calls inside ferrocache
- Use tokio, not async-std
- Every new module gets unit tests before moving on

**Session log rules:**
- Keep only the last 2 session logs below
- When adding a new log, delete the oldest if there are already 2
- Summarize deleted sessions as one-line entries under "Completed work" above

## Section 2: Rolling Session Log (last 2 sessions only)


### 2026-05-03 — Mission 23: Read Repair (Phase 6 close-out)
**Built:** Five concerns wired together into eventual-consistency anti-entropy. (1) **UUID reverse lookup in `SemanticIndex`** — top-level `uuid_to_namespace: HashMap<String, String>` plus a per-namespace `uuid_to_internal: HashMap<String, usize>` (in `NamespacedIndex`). Populated on every `replay_entry` AND `replay_snapshot_entry` so the reverse map survives WAL+snapshot replay. New `get_entry_by_uuid(uuid) -> Option<FullEntry>` is O(1) via the two hops. New public type `FullEntry { uuid, embedding, response, query_text, model_id }`. `insert_with_uuid` got a `uuid_to_internal.contains_key(&uuid)` short-circuit so re-inserting the same UUID is idempotent — important for read-repair retrying and for WAL replay where the same UUID can appear twice (snapshot + tail). (2) **Two new internal endpoints** — `GET /internal/entry/:uuid` (uses `axum::extract::Path`) returns `FullEntryResponse { uuid, embedding, response, query_text, model_id }` or 404. `POST /internal/read-repair` accepts a `ReadRepairRequest` (UUID-mandatory `/insert` variant) → routes through the WAL group-commit channel → `ReadRepairResponse { status, repaired }`. Both bump `read_repair{,_failures}_total` on success/failure paths. Both registered as routes alongside `/internal/entry/:uuid` + `/internal/read-repair`. (3) **`ClusterRouter` additions** — `forward_get_entry(addr, uuid) -> Result<Option<FullEntryResponse>>` (404 → `Ok(None)`, transport errors → `Err`); `forward_query_no_repair(addr, req)` posts to `?local=true&repair=false` so the receiving node doesn't itself fan out; existing `forward_query` updated to send `?local=true&repair=true` so the receiving node KNOWS it's the canonical primary and should repair on miss. (4) **Read-repair flow in `process_query_locally`** — on a local miss, if `state.read_repair_enabled && state.replication_factor > 1 && cluster.is_some() && try_repair`, call `attempt_read_repair`. That function: gets the replica walk, filters out self + Dead peers, fans out via `futures::future::join_all` (added new dep `futures = "0.3"`), takes the first hit, returns it to the caller AND `tokio::spawn`s `perform_repair`. `perform_repair`: `forward_get_entry` from the hitting peer → build `WalEntry` → push through `state.wal.sender()` `WalCommand::Insert` → await oneshot → record metric. Every failure mode (router missing, replica 404, network error, channel closed, index reject) increments `read_repair_failures_total` with a `tracing::warn!`; nothing panics, nothing crashes. (5) **`LocalParam.repair: Option<bool>`** with three-state semantics: `None` defaults to `!self.is_local()` (so a client `?local=true` does NOT trigger repair — it's a diagnostic; but a non-local query DOES); `Some(true)` set by `forward_query` makes the recipient know it's the primary; `Some(false)` set by `forward_query_no_repair` blocks recursion. Wired through the `query_handler` → `process_query_locally(state, req, params.try_repair())` dispatch. (6) **Wiring** — `ClusterConfig.read_repair_enabled: bool` (default `true`, env `FERROCACHE_CLUSTER__READ_REPAIR_ENABLED`); `AppState.read_repair_enabled: bool` plumbed through `AppState::new`; `Metrics.read_repairs_total` + `read_repair_failures_total` + `record_read_repair{,_failure}()`; rendered as `ferrocache_read_repairs_total` + `ferrocache_read_repair_failures_total` counters in `/metrics`; `CountersResponse` (the `/stats` JSON) gained `read_repairs` + `read_repair_failures`; `ClusterStatusResponse` gained `read_repair_enabled: bool`; `ferrocache.toml` gained a commented block. (7) **Tests** — 5 new in `index.rs::tests` (`test_get_entry_by_uuid_found`, `_not_found`, `_cross_namespace`, `test_uuid_to_namespace_populated_on_replay`, `test_replay_dedupes_on_uuid`); 2 new in `router.rs::tests` (`test_forward_get_entry_200`, `test_forward_get_entry_404`); 6 new in `server.rs::tests` (`test_read_repair_endpoint_inserts_entry`, `test_get_entry_endpoint_returns_full_entry`, `test_get_entry_endpoint_404`, `test_read_repair_validates_inputs`, `test_local_param_repair_semantics`, `test_query_local_does_not_trigger_repair_loop`). Updated `test_forward_query_format` in `router.rs` to assert `repair=true` is now in the URL. Updated `test_cluster_status_*` shape tests with the new `read_repair_enabled` field. (8) **Integration script Test 8** — fresh `m23-rr::4` model id (avoiding cross-test residue at threshold 0.90), `?local=true` insert on node1 only with explicit UUID `rr-target`, verify locality (node1 hits, node2 misses), `/internal/entry/rr-target` returns 200 on node1 + 404 on node2, routed query through node2 returns 200 (load-bearing — exact behavior depends on ring layout), `/cluster/status.read_repair_enabled` is `true`. (9) **Docs** — README "Read Repair" subsection under Design Decisions explaining the eventually-consistent rationale + config row in the Configuration table.

**Verified:**
- `cargo test` — 140/140 pass (was 127; +13: 5 index, 2 router, 6 server). All deterministic.
- `cargo clippy --all-targets -- -D warnings` — clean.
- `cargo fmt --check` — clean (after one auto-fmt pass).
- `python3 -m unittest discover tests` — 39/39 pass (no Python changes; M23 endpoints are internal).
- `docker compose up -d --build && ./tests/cluster_integration.sh && docker compose down -v` — 32/32 pass (was 25; +7 M23 assertions).
- **Live read-repair smoke test** on the docker cluster proved end-to-end behaviour: started 3-node cluster, killed node3, did 3 cluster-routed inserts under `rr-stale::4` (lands on node1+node2 only because node3 was dead), restarted node3, queried the same 3 embeddings through node3 → all 3 returned hits via fan-out, and `node3:/metrics` showed `ferrocache_read_repairs_total 2` (the third query routed to a different owner directly, no repair needed). This is the canonical "stale node heals via traffic" scenario and it works exactly as designed.
- Manual `/internal/read-repair` POST → `{"status":"ok","repaired":true}`; immediate `/internal/entry/{uuid}` GET returns the same entry; `read_repairs_total` increments by 1 on the call.

**Key decisions:**
- **`?local=true` defaults to no-repair** for client-driven requests, but `forward_query` sets `repair=true` explicitly so the forwarded recipient (the actual primary) DOES fire read-repair on miss. This three-way distinction (None = "depend on local", Some(true) = forwarded-to-primary, Some(false) = read-repair fan-out) lets us share the `?local=true` flag for both "skip ring routing" AND read-repair gating without ambiguity.
  - The first version of this had `try_repair` default to `true` regardless of `local`, which caused integration test 5 to fail: a `?local=true` query from the test client triggered fan-out + read-repair, polluting node2's local index with data the test was checking it lacked.
- **Repair re-inserts via the WAL group-commit channel**, not direct `index.write()`. This gives repaired entries the same fsync durability as organic inserts — a node that crashed mid-repair won't re-miss the same query forever; on restart, the repaired entry is already on disk.
- **`tokio::spawn` for the repair half**, returning the hit synchronously. Client gets the lowest-latency response (single replica RTT, not RTT + WAL fsync); the repair runs concurrently and is fire-and-forget. If it fails (replica went down between the query and the GET), the metric increments and the next miss tries again.
- **De-dupe on UUID inside `insert_with_uuid`**, not at the read-repair layer. Two reasons: (a) read-repair could fire concurrently on the same entry from multiple coordinators, and the WAL channel doesn't sequence between them; (b) WAL replay already has the same shape — snapshot + tail can both contain the same UUID (snapshot up to seq N, WAL tail starting at N can include a re-insert before compaction). One check at the lowest level handles both.
- **`futures` crate, not hand-rolled `tokio::join!`**. `tokio::join!` is fixed-arity at compile time; `futures::future::join_all` takes a `Vec<Future>` so the fan-out scales with the replica count. Standard, well-maintained crate, ~10KB compiled. Not worth rolling our own polling combinator.
- **No anti-entropy on insert**, only on query. The brief was clear: read-repair only. The asymmetry is fine because inserts are write-replicated to the right places by M6's fan-out — a missed insert lands on N-1 replicas (degraded by M21/M22 but still some); subsequent queries from anywhere fan out and find the data. Build-time is bounded by the read-repair window of "next query for this entry" rather than by a periodic scan.
- **`/internal/entry/:uuid` exposes the full entry including the embedding** (not just the response). Read-repair needs the embedding to insert into HNSW; a coordinator that only had `(response, query_text, model_id, uuid)` would have to recompute the embedding from `query_text`, which it can't (the original embedding model is client-side, not in ferrocache). So the embedding has to be on the wire — there's no way around it. For a 384-dim f32 embedding that's 1.5KB, JSON-encoded ~2-3KB; acceptable for a one-shot fetch on cache miss.
- **De-dupe on UUID short-circuits BEFORE incrementing `next_id`**. So calling `insert_with_uuid` twice with the same UUID doesn't waste an internal id. Side benefit: snapshot-then-WAL-replay where the same UUID appears in both doesn't grow the side-table.

**Deviations:**
- Brief sketched `forward_get_entry` to return `Option<FullEntryResponse>`. Returned `reqwest::Result<Option<FullEntryResponse>>` — surface the transport error so `perform_repair` can bump the failure metric on connection problems but treat a 404 as `Ok(None)` (cleaner pattern-match call site).
- Brief had read-repair handler always set `repaired: true`. Implemented: `Ok` → `repaired: true`, errors return error responses (not `repaired: false`). Reasoning: a dimension-mismatch error from the index layer means the entry was rejected and is genuinely not repaired; 200 + `repaired: false` would lie to the caller.
- Brief proposed `/internal/entry/{uuid}` always succeeding, but I added validation + 404. axum routes `:uuid` matches anything, including empty paths — graceful 404 + `ErrorResponse { error: "entry not found" }` is cleaner than letting a malformed UUID 500.
- Brief showed `tokio::spawn` of a closure with `state.clone()` etc. Implemented with named `perform_repair` async fn for testability + readability. Same effect.
- Brief's integration test 8 used the same `test-model::4` namespace as earlier tests. That model's namespace gets entries with similar embeddings during tests 5/7 that hit at threshold 0.90 against arbitrary 4-dim vectors (cosine sim is generous when most components are 0). Switched to a fresh `m23-rr::4` namespace so the M23 test's "node2 doesn't have it yet" assertion isn't poisoned by residue from tests 5 + 7.
- Brief proposed a `test_read_repair_on_query_miss` server test using a mock router. Skipped — the logic is exercised via (a) endpoint tests, (b) the integration script's live failover-then-rejoin path, and (c) the live smoke test. A unit-level mock would have to construct a `ClusterState` (which requires UDP), and mocking that out adds complexity for less coverage than the integration tests already provide.
- Brief had `perform_repair` take `peer_addrs: Vec<(String, String)>` and try each one. Implemented with the single peer that already returned the hit (we know it has the entry). Simpler, faster; if that one peer is now unreachable for the GET, we just record a failure and move on.
- Brief proposed adding `read_repairs_total` increments to BOTH `attempt_read_repair` (when the hit is returned) and `perform_repair` (when the local insert succeeds). Picked just `perform_repair` — the metric tracks "repairs that actually completed and are on disk locally", which is what dashboards want. Returning a hit from a replica is just a query; only the local-insert step is a "repair".

**Open / next:**
- **No durability guarantee on the spawned repair task.** If the process crashes between the spawn and the WAL fsync, the entry is gone (until the next query refires the cycle). For a cache this is fine; for a DB this would need explicit write-ahead of the repair intent. The brief explicitly said "this is acceptable for a cache" — a miss just means re-call the LLM.
- **The repair traffic isn't rate-limited.** A flood of misses on a stale node could spawn a flood of background tasks. Tokio's runtime caps concurrent tasks via the cooperative scheduler, but unbounded growth is theoretically possible. A future addition: a `Semaphore` of N concurrent repairs (e.g. 100) so a runaway scenario is bounded.
- **No measurement of repair latency.** `perform_repair`'s wall-clock time isn't recorded. Adding a `read_repair_duration_seconds` histogram would let operators see how long repairs take in practice (and alert if it grows). Trivial to add later.
- **`/internal/entry/:uuid` linear-scans across the same namespace via the `uuid_to_internal` HashMap** (so it's actually O(1)). The reverse map adds memory: ~80 bytes per entry on a 64-bit system (HashMap overhead) plus the UUID string itself. For 100K entries that's ~8MB, negligible. For 100M entries it'd be 8GB and worth reconsidering — but ferrocache is a node-local cache, not a sharded big-data store, so this is fine at design scale.
- **The fan-out queries replicas in parallel but takes the FIRST `Ok(hit)`**. If a slow replica hits first, we wait for it. If a fast replica misses first, we still wait for the slow one in case it hits. A future optimization: stop the join early once any hit comes in (`select` over the futures). For replication_factor=2 with one peer per fan-out, this is moot.
- **No tests against the actual production cluster shape.** All tests run in-process or against the docker-compose 3-node setup. Read-repair correctness against "100-node cluster + 50% packet loss" is not exercised here. The Phase 6 portfolio scope doesn't require it.
- **Phase 6 is the final phase as currently planned.** ferrocache now has: 140 Rust tests + 39 Python tests + 32 integration assertions passing; group-commit + phi-accrual + ring failover + read-repair all opt-in or default-safe; documented end-to-end. Future work is operator-driven hardening (real workloads finding real edge cases) rather than feature-driven.

### 2026-05-03 — Mission 24: Per-Entry Access Tracking (Phase 7 kickoff)
**Built:** Three new tracking fields wired through the entry lifecycle. (1) `CacheEntry` (in `src/index.rs`) gained `inserted_at: u64`, `last_accessed_at: u64`, `access_count: u64`. `inserted_at` is set once at insert and persisted in the WAL; the other two are in-memory soft state, persisted only via snapshots (a WAL write per query hit would defeat the cache). (2) `WalEntry` gained `inserted_at` with `#[serde(default)]` for legacy compatibility; `SnapshotEntry` gained all three with `#[serde(default)]`. (3) New `pub fn now_unix_secs()` helper in `index.rs`. (4) `NamespacedIndex.insert_with_uuid` signature grew three params (`inserted_at`, `last_accessed_at`, `access_count`); marked `#[allow(clippy::too_many_arguments)]` because the alternative — a builder or struct param — adds boilerplate to two production callers and adds nothing to readability. (5) `QueryHit` gained `internal_id: usize` so the handler can call `record_access` without a second lookup. (6) New `SemanticIndex::record_access(model_id, internal_id)` and `NamespacedIndex::record_access(internal_id)` use `entries.get_mut` + `saturating_add`; called from `process_query_locally` under a brief write lock that immediately drops. The read lock for the actual HNSW search is dropped first, then the write lock is held just long enough to bump two fields. Misses skip this entirely. (7) Insert path: `local_insert_inner` stamps `inserted_at = now_unix_secs()` before sending the `WalCommand::Insert`; `read_repair_handler` stamps the current time too (the public ReadRepairRequest predates M24); `perform_repair` (read-repair fan-out) preserves the source's `inserted_at` from the `FullEntryResponse` so a re-joined node doesn't see all repaired entries as freshly created. (8) `replay_entry` reads `inserted_at` from the WAL (defaults to 0 for pre-M24 lines) and sets `last_accessed_at = inserted_at`, `access_count = 0`. `replay_snapshot_entry` restores all three from the snapshot. (9) `FullEntry` + `FullEntryResponse` gained the three fields so read-repair carries access metadata over the wire. (10) `NamespaceStats` gained `oldest_entry_ts`, `newest_entry_ts`, `total_accesses` (O(n) scan in `namespace_stats()` — fine for an admin endpoint); `NamespaceStatsEntry` (the JSON shape) gained the same three. (11) New `SemanticIndex::top_entries_per_namespace(N) -> HashMap<String, Vec<TopEntrySummary>>` sorts each namespace's entries by `access_count` descending and truncates to N; powers the new `GET /admin/entry-stats` endpoint. The endpoint returns `EntryStatsResponse { namespaces: HashMap<model_id, NamespaceTopEntries { top_entries: Vec<TopEntryRow> }> }` where each row is `{ uuid, access_count, last_accessed_at, query_text_preview }` (preview is first 50 chars + "..." if longer). (12) `query_text_preview` is char-aware (`s.chars().count()` / `s.chars().take(50)`) so multi-byte UTF-8 inputs don't slice mid-grapheme. (13) Tests: 8 new in `index.rs::tests` (`test_insert_sets_inserted_at`, `test_insert_sets_initial_access_fields`, `test_record_access_increments_count`, `test_record_access_updates_last_accessed`, `test_inserted_at_does_not_change_on_access`, `test_namespace_stats_access_fields`, `test_snapshot_preserves_access_fields`, `test_replay_snapshot_preserves_access_fields`); 2 new in `wal.rs::tests` (`test_wal_inserted_at_roundtrip`, `test_wal_legacy_entry_defaults_inserted_at_zero`); 4 new in `server.rs::tests` (`test_query_hit_updates_access_count`, `test_query_miss_does_not_update_access`, `test_admin_entry_stats_endpoint`, `test_stats_includes_access_fields`). All existing test fixtures updated with `inserted_at: 0` defaults — `wal.rs`, `snapshot.rs`, `server.rs`, `metrics.rs`, `router.rs`, `benches/cache_bench.rs`. (14) Refactored `wal_entry()` test helper in `index.rs::tests` so the new field is set in one place rather than 8.

**Verified:**
- `cargo test --lib` — 154/154 pass (was 140; +14 new). All deterministic.
- `cargo clippy --all-targets -- -D warnings` — clean.
- `cargo fmt --check` — clean (one auto-fmt pass folded a long fn signature and a multi-line `assert!`).
- `python3 -m unittest discover tests` — 39/39 pass (no Python changes; new fields are server-side serde additions).
- `docker compose up -d --build && ./tests/cluster_integration.sh && docker compose down -v` — 32/32 pass (M22 + M23 paths still green; no new integration assertions added — M24 is observable via `/stats` + `/admin/entry-stats`, not via cluster behaviour).
- **Live smoke test** with the release binary: 3 inserts (`u1`, `u2`, `u3`), 5 queries against `[1.0, 0.0, 0.0]`. `/admin/entry-stats` returned `top_entries[0] = { uuid: "u2", access_count: 5, ... }` (HNSW returned u2 first because both u1 and u2 lie on the +x axis with cosine sim 1.0 — first-wins, expected); `top_entries[1]` and `top_entries[2]` had `access_count: 0`. `/stats.namespaces["smoke::3"]` showed `total_accesses: 5`, `oldest_entry_ts == newest_entry_ts` (all three inserted in the same second), `entry_count: 3`. `/internal/entry/u1?local=true` returned the full record with `inserted_at` populated and `access_count: 0` (never the HNSW winner for that probe). Long preview was truncated correctly with the trailing `...`.

**Key decisions:**
- **`access_count` and `last_accessed_at` are NOT in the WAL.** A WAL write per query hit would invert the cache's central performance property — every hit becomes a fsync, query throughput collapses to insert throughput. Snapshot persistence is enough because the operationally-important number is the relative ranking ("which entries are hot"), not the absolute count. After a crash + WAL-only replay, hot entries quickly re-establish their lead.
- **Brief write lock on every hit, not atomics.** The brief explicitly weighed and rejected the AtomicU64 path: serde can't derive for atomics, snapshot/WAL roundtripping becomes ugly, and the lock is held for ~5 instructions. Tokio RwLock isn't writer-preferring, so concurrent readers aren't starved by access updates. Misses skip the lock entirely (the most common path on a cold cache).
- **`QueryHit.internal_id` is part of the public surface, not added through a separate lookup API.** The handler already gets the internal id back from HNSW; threading it into the QueryHit avoids a second `entries.get` or `uuid_to_internal.get` to find the entry on hit. Tiny optimization, but the alternative was uglier.
- **`inserted_at` defaults to 0 for legacy entries (pre-M24 WAL + pre-M24 snapshots).** This is intentional — when M25 lands, `inserted_at == 0` entries will sort as oldest under LRU, which is correct for entries that predate access tracking. They'll get evicted first if the cache fills, which is what an operator wants ("the new code knows about access; old data is fungible").
- **`#[serde(default)]` on bincode-encoded `SnapshotEntry` fields.** Bincode 1 doesn't natively support skipping missing fields, but a snapshot mismatch fails the read with a `bincode::Error` and the existing fallback path (corrupt-snapshot → full WAL replay) catches it. New deployments and the fresh test scenarios work; old snapshots are gracefully reconstructed from the WAL. Acceptable.
- **`/admin/entry-stats` is full sort, not heap-based.** O(n log n) per namespace, where n is the namespace's entry count. For 100K entries that's ~1.7M comparisons — sub-millisecond on any laptop. A bounded heap would shave that to O(n log 10), but this isn't a hot path; clarity wins over micro-optimization. If a real workload pushes a single namespace past 1M entries, the sort can be replaced.
- **`record_access` is a method on `SemanticIndex`, not just `NamespacedIndex`.** The handler doesn't know about namespaces below the top level (it has the `model_id` string and the `internal_id`). Adding the layer of indirection at the top is the natural place — keeps `NamespacedIndex` an internal implementation detail.
- **Read-repair preserves `inserted_at` from the source replica, but resets `last_accessed_at` and `access_count` to 0.** Three reasons: (a) the WAL doesn't carry the latter two, so we can't write them durably anyway; (b) the new owner's view of "hot" is its own — what was hot on the replica isn't necessarily hot here; (c) `last_accessed_at = inserted_at = source's inserted_at` is honest about when the entry was first created in the cluster, and `access_count = 0` on the new owner is honest about local activity. Future LRU on the new owner will correctly reason about both.
- **`now_unix_secs()` returns 0 if the system clock predates UNIX_EPOCH.** Impossible on any sane host, but `unwrap_or(0)` beats panicking. Result: a clock-skew bug becomes "stamps are 0", which falls into the same legacy bucket and gets evicted first under LRU. Fail closed, not crash.
- **Integration script unchanged.** M24 is observable via `/stats` and `/admin/entry-stats`, both of which are infrastructure endpoints — they don't have cluster-level invariants to assert. The smoke test covers the wire-level shape; cluster_integration.sh covers cluster-level behaviour (M21–M23 paths still green).

**Deviations:**
- Brief proposed taking the index write lock immediately after the read lock dropped. Implemented exactly that, but pulled the bump into a fresh block scope (`{ let mut idx = state.index.write().await; idx.record_access(...); }`) so the write guard drops before the response is built. Avoids holding the write lock through `Json(...)` serialization.
- Brief sketched `record_access` as `if let Some(ns) = ... { if let Some(entry) = ... { ... } }`. Same logic, but split into two methods — `SemanticIndex::record_access` and `NamespacedIndex::record_access` — so the access bump is a single call site at each level. Cleaner under cargo fmt.
- Brief had `query_text_preview` use byte slicing (`&s[..50]`). Replaced with `chars().take(50)` so a UTF-8 multi-byte sequence at byte 50 doesn't panic. Single line change, prevents a class of silent bugs.
- Brief proposed `InsertResponse` carrying `inserted_at`. Skipped — the API contract didn't ask for it, callers don't need it (they have their own clock), and `/internal/entry/{uuid}` already returns it. Smaller wire surface.
- Brief had a `test_admin_entry_stats_endpoint` that just checked the field exists. Strengthened to insert 3 entries with controlled access counts (3, 1, 0) and assert `top_entries[0..2]` are in the right order. Locks in the sort behaviour, not just the shape.
- Brief proposed a `record_access_idempotent` test ("calling twice with the same internal_id increments by 2, not 1"). Folded into `test_record_access_increments_count` — same coverage, fewer test functions.

**Open / next:**
- **M25 is LRU eviction.** With `last_accessed_at` and `access_count` now wired through, LRU is just "find the entry with the smallest `last_accessed_at` (or build a min-heap of them) and remove it when the namespace exceeds N entries." Per-namespace eviction makes more sense than global because different model_ids may have very different working-set sizes. The HNSW removal path is the unknown — `hnsw_rs` doesn't expose a delete API; we may need to mark entries as tombstoned and rebuild the HNSW periodically (or accept that eviction degrades the HNSW until the next compaction).
- **The /admin/entry-stats endpoint is auth-gated like everything else under `/admin/`.** The brief was clear it should follow the same auth pattern; existing `auth_middleware` covers it because it's not in the exempt list (`/health`, `/metrics`).
- **No measurement of `record_access` overhead.** The brief asked to capture any query benchmark change in the session log. `cargo bench` was not run in this mission (the existing benches don't go through the HTTP handler, they call `idx.query` directly, which doesn't touch `record_access`). A fresh benchmark would need to drive the server end-to-end — out of scope here, but useful to add when M25 lands.
- **Bincode incompatibility risk for old snapshots.** `#[serde(default)]` on bincode-encoded fields is wishful — bincode 1 deserializes positionally, not by field name. Pre-M24 snapshots will fail to deserialize the new `SnapshotEntry` shape and trigger the existing "corrupt snapshot → full WAL replay" fallback. Cleanly handled, but worth flagging: the snapshot format is now incompatible with pre-M24 binaries. A snapshot version bump (currently `VERSION = 1`) would make this explicit; not done here because the existing fallback covers the case.
- **Entries with `access_count == u64::MAX` saturate.** `saturating_add` means a cache entry that has been hit ~1.8e19 times stops counting. Not a real-world concern — at 1µs per hit, that's 580K years of constant traffic. Recorded as a curiosity, not a TODO.
- **`oldest_entry_ts` / `newest_entry_ts` are O(n) in `/stats`.** For a namespace with 1M entries, every `/stats` scrape iterates all entries. Could be cached and updated lazily, but `/stats` is a low-frequency admin endpoint; not worth the bookkeeping. A future operator who wants high-frequency `/stats` polling would benchmark and decide.
